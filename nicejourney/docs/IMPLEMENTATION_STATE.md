# Nice Journey — Implementation State

Updated: 2026-09-20

> Navigation note: this is the durable implementation record and is intentionally large. For normal tasks, use `docs/INDEX.md`, search this file for the subsystem/wave/asset/test you need, and read only the matching section. Read end-to-end only for an explicit state audit.

## Authority

Implementation follows `../../Nice_Journey_Master_Game_Specification_V2_1.md` and `../../astra_guide.md`.

## Decision register status — resolved 2026-09-14

DR-01 through DR-08 are now **approved and resolved** in the master specification §58 by direct developer instruction. Existing neutral/data-boundary slices remain valid, but production implementations of the newly approved decisions are still pending unless explicitly listed elsewhere in this state file. Numeric balance, timing and magnitude values that the master marks as tuning/playtest variables remain tunable without reopening the underlying mechanic.

Approved directions: DR-01 persistent floor revisits with coherent failed-attempt rollback and no renewable combat farming; DR-02 whole-state safe-snapshot death rollback with no death fee; DR-03 exactly 15 quest definitions with the Region 3 → permanent Tower Sigil + Floor 1 unlock chain inside the Floor 1 primary; DR-04 renewable starter kits with resource-free basics, no Ranged ammo, Mage-only mana and Melee shield block/parry; DR-05 three active + three passive skills per class with learned 2+2 safe-location loadouts and deterministic temporary-skill restoration; DR-06 the shared readable block/parry/mitigation/weak-point/poise/Burn/Slow ruleset; DR-07 the two-phase no-add Floor 10 boss **The Tenth Warden**; DR-08 the named integrated-GPU minimum class and measurable performance gate defined in §58.

## Engine / renderer baseline

- Production compatibility requirement: Godot 4.5+.
- Current pinned local development engine: **Godot 4.6.2 stable Mono**, build `71f334935`.
- Initial renderer baseline: **GL Compatibility**.
- Live launch evidence: OpenGL 3.3 Compatibility on Intel Iris Xe Graphics.
- The renderer choice remains subject to later profiling and DR-08 minimum-hardware acceptance; it is not a final performance claim.
- World canvas: 640×360.
- Window override: 1280×720.
- Stretch: canvas items, keep aspect, integer scale mode.
- Physics: 60 Hz.

## 2026-09-20 three-class finite-route live integration QA

- `tests/test_three_class_required_route_live_qa.gd` now runs the shipped `GameplayRoot` through **all ten floors for each of Melee, Ranged and Mage**, using real Quest Hall Sigil preparation, Tower travel, durable entry checkpoints, physical exit triggers and once-only Quest Hall XP/Level-10 progression. Floors 1–9 activate their real encounter residents and each class damages the first resident of every encounter with its starter basic action through the ACTIVE window, world hit geometry (projectile travel for Ranged/Mage), mitigated HP and normal action recovery; remaining resident HP defeats use the shared resolver with controlled finishing damage. Floors 2–9 activate the authored production encounters through room triggers; their quest bindings complete defense waves on 3/7/9 and annihilation on 4/5. Physical Escort actors on 2/6/8 traverse their authored ordered waypoints to a live goal with HP remaining. Floor 10 defeats seven real residents (including the three separately authored elites) outside the boss, activates the shipped Sanctum, lands each class's normal basic attack on the live Warden HP via its actual melee/projectile world delivery, and applies additional boss HP damage through the shared contact resolver, observes the irreversible <=50% phase transition through the live action clocks, defeats the boss and lets the Sanctum terminal event/production progression bridge complete the primary. No test writes `ObjectivesComplete` or invokes the boss-result service directly. Each class banks all ten outcomes through the real floor exit and Quest Hall, checks the 4,500-XP Level-10/9-point cap, save/load continuity, duplicate-claim rejection and no Floor 11.
- **Evidence boundary:** Deterministic test positioning/aim and intentionally fatal attack payloads still finish residents and boss; the route proves real combat HP/quest/boss ownership, but **does not** prove a normal-damage, fully survived, human-input three-class run or full encounter difficulty. Its physical Escort actors complete the route after the test clears resident encounters; this does not establish simultaneous live combat while escorting. Defense objectives verify the real required wave roster's defeat and surviving objective HP but do not assert incoming objective damage under pressure. The boss route verifies phase transition and terminal progression, while dedicated tests own detailed five-move/Arc Volley/weak-point/defense behavior. Numerical balance, telegraph geometry/readability, fairness and final Inspector production values remain manual acceptance pending. The test uses a process-specific save root to avoid headless-gate slot races; action clocks finish naturally before the next safe checkpoint.
- **Verification:** The revised isolated Godot 4.6.2 Mono headless route test passed for all three classes and all 30 floor-entry/exit/turn-in cases, including a real basic HP hit on the first resident of every Floor 1–9 encounter. The subsequent local working-tree `tools/reference_validation.ps1` passed **273 discovered tests, 0 failures**, headless editor parse, main-scene smoke and GL Compatibility renderer-active reference smoke (`docs/evidence/reference-validation-latest.json`, generated `2026-09-20T08:14:11Z`; `final_hardware_acceptance=false`). This gate also runs the existing class skill/defense, status player/enemy parity and persistence, Warden contact/phase/projectile, Escort, death/retry, save/restore and once-only reward tests. The reference gate ran with *concurrent uncommitted art changes and their new test*, so its 273-case manifest is **not** claimed as the last remote-commit baseline; resource-UID text-path fallback warnings occurred, and the intentional `invalid profile_id` negative fixture logged without a test failure. The Auvrynt editor bridge was disconnected, so live editor-bridge input/screenshots/manual visual acceptance were not performed. Unrelated art changes were preserved.

## 2026-09-20 Burn/Slow, Tenth Warden and Region 3 map code-only authoring closure

This entry supersedes older missing-validation and incomplete-map-snapshot descriptions. DR-06, DR-07 and DR-02/03 authorize the mechanics; they do **not** approve final numerical status/boss balance or exact Region 3 checkpoint/quest marker tiles and hostile Recommended Levels. No new workers, generated assets, exports or fabricated final data were used for this slice.

- **Burn/Slow:** `tools/create_status_playtest_tuning.gd` now reproduces the shipped `StatusPlaytestTuning` class and exact current Inspector **PLAYTEST** 180-tick Burn (30-tick interval, 2 HP/tick) and 150-tick Slow (25% reduction, 40% floor); the former generator could silently replace live content with obsolete values/class. `StatusPlaytestTuning` rejects malformed sources, fractional/nonfinite durations and magnitudes, preserves strongest-value/longest-refresh source-scoped behavior, and keeps Burn ticking after longer same-ID duration extension. New player/enemy status parity tests cover two independent sources, real HP/movement and expiry. `StatusProductionTuning` supplies an *unassigned* Inspector authoring/readiness template; `PrototypeStatusProductionAuthority` stays `production_ready=false` until actual approved content and a live production owner exist. Burn damage/cadence/duration and Slow duration/reduction/floor are **not** final simply because tests pass.
- **The Tenth Warden:** The shipped `tenth_warden_playtest_v01.tres` keeps explicitly provisional HP/stamina/defenses/poise, five Inspector move action clocks/costs, payloads/shape geometry, two-phase cadence and weakness timing. `TenthWardenProductionAuthoring` now rejects unpayable/nonfinite move costs, costed moves without regeneration, negative defense, missing startup telegraphs, heavy Phase-2 moves without recovery, same-tick Arc Volley projectile launches and costed/cooldown-gated/cancellable irreversible phase transitions. `TenthWardenSanctum` checks actual projectile scene/travel and movement geometry; `TenthWardenEncounterController` requires the exact authored ACTIVE tick for each authenticated melee contact, including separate Twin Cut strikes, without changing traveling projectile impact semantics. Exactly two phases/five move families, no adds/healing, finite victory, no extra reward/Floor 11 remain unchanged. Final boss damage, geometry, timing and stats still require three-class manual telegraph/hit fairness and production approval.
- **Region 3 map:** Existing five quest IDs have zone/Quest Hall identities but `exact_tile_authored=false`; no fixed checkpoint anchors are authored; the four hostile Recommended Levels stay `-1` (master gives only provisional 1–2 / 2–4 / 4–7 / 8–10 bands). Layout readiness now checks exact markers/checkpoints for duplicates, missing coverage, invalid tiles and wrong-zone coordinates before declaring geometry available. The Region map *separately* shows a validated, already committed `profile.safe_state` location within a physically explored zone at exact source-backed tile coordinates, with a distinct marker and text. This saved historical player position is neither a fixed authored Region checkpoint nor a teleport destination; invalid/missing discovery/revision/position and forged travel/fixed-checkpoint facts fail closed. No quest tiles, fixed checkpoint coordinates or exact danger levels were invented.
- **Verification:** Godot 4.6.2 stable Mono `tools/reference_validation.ps1` discovered **271 tests, 0 failures**, with headless editor parse, main-scene smoke and GL Compatibility renderer-active reference smoke. Evidence: `docs/evidence/reference-validation-latest.json`, generated `2026-09-20T05:27:31Z`, `final_hardware_acceptance=false`. Focused checks include `test_status_player_enemy_parity.gd`, `test_status_production_inspector_authoring.gd`, `test_tenth_warden_contact_tick_admission.gd`, `test_region3_saved_safe_map_location.gd` and `test_region3_saved_safe_ui.gd`. The recurring `Save rejected: invalid profile_id` log is the intentional negative save fixture, not a suite failure.

## 2026-09-20 Region 3 economy / A-B-C side quests / twelve enemy archetypes code-only closure

This entry supersedes older statements that these PLAYTEST systems lack live authoring/validation. Master §28/§62 and §58 approve mechanics, roles and boundaries, **not** final numeric stock/prices, recipes, recovery amounts, wave/reward/difficulty balance, or enemy stats/XP/Gold. All shipped values remain explicitly PLAYTEST; absent role-level reward and difficulty values remain unauthored rather than fabricated.

- **Region 3 economy integrity:** The live `region3_services_playtest_v01.tres` presently declares General Merchant ore, tonic, and the three starter weapons plus Clinic tonic. `Region3PlaytestContent` now checks one 0→1 upgrade recipe per approved starter weapon, each currently merchant-sourced material/available initial quantity, valid and category-appropriate Clinic stock, supported nonduplicated recovery target mappings, and integral HP recovery. `VendorStockCatalog` rejects cross-vendor buy→sell profit cycles in addition to existing per-vendor safety. Actual purchase, starter-upgrade, Inn/Clinic recovery and atomic save/rollback remain on their established live owners. Final stock/quantities/prices, Gold/material costs, upgrade magnitudes and recovery values still require production approval and finite-economy/poor-drop playtest evidence.
- **Side quests A/B/C:** The live provisional Escort A has three enemies, Annihilation B five, Defense C three two-enemy waves, and independently claimed Quest Hall XP 40/60/80. Side-quest content validation rejects unsupported A/B waves, inappropriate route or retry/leave policy IDs and nonintegral XP; authored anchor coverage derives from the resource rather than a baked 3/5/6 marker gate, while attempt binding requires exact physical marker/wave agreement. `Region3SideQuestRuntime` validates an Inspector enemy-stat catalog and uses each canonical archetype's HP/stamina/physical+arcane-defense/poise modifiers in real regional encounters, matching the Tower role calculation without inventing a floor scale. Partial progress → preview/leave/retry, failed-attempt restore, complete-once XP and missing authoring are covered in focused live tests. Numerical counts, defense/escort HP, final difficulty, recovery and retry/leave balance still require content acceptance; nonrepeatable claim ownership remains unchanged.
- **Twelve enemy roles:** New `enemy_archetype_stats_playtest_v01.tres` and role-stat catalog supply exactly twelve independently Inspector-editable, validated PLAYTEST health, stamina, physical/arcane defense and poise modifiers, over the existing floor and elite baseline. Shipped neutral modifiers intentionally preserve current resident stats until differentiated production data is approved. The Tower factory and Region 3 quest runtime consume the same per-role data. `EnemyPlaytestAttackCatalog` requires the nine distinct damaging role signatures plus three genuine non-damage signatures: Support ward, Summoner finite reinforcement and Controller Slow. Per-archetype difficulty, XP and Gold retain explicit unauthored sentinel `-1`; no per-kill reward caller, renewable farming or final numeric attack/stats is invented. Dedicated tests prove role isolation, live elite/stat application and invalid attack/category rejection.
- **Verification:** Godot 4.6.2 stable Mono `tools/reference_validation.ps1` on 2026-09-20 discovered **266 tests, zero failures**; headless editor parse, main-scene smoke and GL Compatibility renderer-active reference smoke passed. Evidence: `docs/evidence/reference-validation-latest.json`, generated `2026-09-20T05:04:07Z`; `final_hardware_acceptance=false`. Focused new/regressed tests include `test_region3_playtest_economy_content.gd`, `test_economy_vendor_stock_catalog.gd`, `test_region3_live_side_quest_attempts.gd`, `test_region3_live_side_quest_host.gd` and `test_enemy_archetype_playtest_stats.gd`. The recurrent `Save rejected: invalid profile_id` log is the intentional negative save fixture, not a suite failure.

## 2026-09-20 player progression / active-rank / passive-runtime code-only closure

This entry supersedes older assertions that skill ranks 2–3 or passive runtime bonuses are not implemented. The resolved §19/§58 mechanical contracts are live; no designer-approved final XP, class growth, skill cooldown/damage/geometry or rank magnitudes have been supplied. All numeric content below remains explicitly Inspector-authored **PLAYTEST** tuning, not final production authority.

- **Progression safety:** The shipped `progression_playtest_v01.tres` still authors the reversible 100 × current level XP step (4,500 cumulative Level-10 cap), 80 × floor number primary Quest Hall XP, a separately claimed 160-XP Floor-10 boss reward, side A/B/C 40/60/80 XP, 100 HP/stamina per-class bases and nine +8 HP/+2 stamina/+1 skill-point transitions. The existing 4,560-XP required route remains finite, and no Level 11 or renewable reward was added. `LevelProgressionProfileCommitService` and `PlaytestQuestXpCommitService` now roll back even partially applied failed runtime-stat callbacks, verify applied stat readback before a durable save, and verify rollback readback after failed saves. Rejected or falsely successful callbacks cannot bank a claim or strand mutated player stats; dedicated progression profile/Quest Hall regressions cover retry after rejection.
- **Nine active skill ranks:** `active_skills_playtest_v01.tres` now carries all nine explicit rank-2/3 Inspector adjustments to magnitude, cost and recovery. `ActiveSkillsPlaytest` validates finite monotonic improvements and rejects missing/invalid adjustments; `SkillExecutionService` resolves the learned equipped rank into a cloned action, while `PlayerPlaytestAttackDelivery` applies rank-appropriate effects to real hits and Aegis Ward duration. The canonical skill identity, cooldown, geometry, active/commit semantics and 2+2 loadout remain intact. Shipped player-attack PLAYTEST resources now author poise for the three damaging Melee skills; normal enemy/boss contacts use the existing DR-06 poise owner and deduplication. Projectiles and pulses preserve launch-origin information for per-target long-range effects, and boss weak-point amplification is applied only to authenticated visible exposure. The HUD reads rank/passive-adjusted resource cost and recovery rather than base-only values. This code/data path is playable; final per-skill attack resource approval and numeric combat balancing remain separate.
- **Nine passive runtime effects:** The Inspector-assigned `passive_skills_playtest_v01.tres` defines three provisional ranks for all approved passives; `PassiveSkillRuntime` derives effective bonuses only from the equipped, learned profile slots and rebinds on load, Training Hall rank/swap, and death/retry. Efficient Footwork modifies Melee action and burst stamina costs; Breaker modifies actual authored poise pressure; Parry Recovery restores stamina once for an authenticated successful parry; Longshot modifies distance-qualified damage; Fleet Recovery modifies Ranged movement stamina cost and stamina regeneration; Expose affects a verified visible boss weak point; Mana Weave reduces Mage action mana cost; Flow Recovery modifies Mage regeneration from its unmodified authored baseline; Stable Casting shortens Mage active-skill recovery without changing commitment. Source actions, base stats and profile ownership are never compounded or silently rewritten. Rank previews and live class/action, movement, mana, parry, load/save and invalid-authoring checks are covered by focused tests.
- **Verification:** Godot 4.6.2 stable Mono `tools/reference_validation.ps1` on 2026-09-20 discovered **264 tests, 0 failures** and passed headless editor parse, main-scene smoke and GL Compatibility renderer-active reference smoke (`docs/evidence/reference-validation-latest.json`, generated `2026-09-20T04:42:24Z`). Focused tests include `test_active_skill_rank_playtest.gd`, `test_passive_skill_runtime_playtest.gd`, `test_gameplay_passive_live_integration.gd`, `test_gameplay_active_skill_clock_playtest.gd`, `test_player_active_skill_distinct_effects.gd`, `test_gameplay_quest_xp_playtest.gd`, and `test_gameplay_training_hall_skills_flow.gd`. `Save rejected: invalid profile_id` is the existing intentional negative fixture, not a gate failure. This reference-machine smoke is not final DR-08 hardware acceptance.

## 2026-09-20 code-only economy / Region 3 exploration / UI closure

This entry supersedes older pending-runtime/validation descriptions for these areas. Existing service prices, recovery values, regional levels and art remain explicitly provisional or unauthored where stated; do not infer production balance or approved geography from a green code gate.

- **Economy integrity:** Shipped Inspector-assigned `region3_services_playtest_v01.tres` already provides General Merchant and Clinic stock/prices, three starter weapon upgrade recipes, Inn/Clinic recoveries and mapped atomic save/rollback. `VendorStockState` now rejects persisted near-integer fractional price/quantity values rather than rounding then silently truncating during JSON load, and enforces the live 2,147,483,647 numeric bound. Economy JSON, stock, merchant, Blacksmith, recovery and shipped-service focused gates pass. Final production costs, inventory stock, and economy pacing remain unapproved.
- **Region 3 danger/data:** `Region3WorldZoneDefinition` now exposes optional exact Inspector-authored `recommended_level` (default -1); a hostile value must lie inside its existing master-authored level band, while safe zones cannot acquire a numeric danger level. `Region3MapSnapshotService` produces deterministic risk markers only for physically discovered hostile zones after all four hostile recommendations are explicitly valid, using current player level and existing `DangerEvaluator` ranks/confirmation rules on every read. Missing exact values retain `region_danger_concrete_level_unauthored` and an empty risk view; the shipped layout still authors no guessed exact recommendations. Exact quest marker tiles and regional checkpoint coordinates also remain unapproved/unavailable. Focused world-layout/map/discovery tests pass.
- **UI/UX and accessibility:** Merchant now shows quantity/total versus owned Gold and disables an unaffordable/out-of-stock Buy before submitting to authoritative transaction callbacks. Blacksmith read-only preview computes exact missing inventory materials and Gold and disables unavailable upgrades; transaction checks stay authoritative. Quest Log retains the selected pending reward claim across profile refresh and falls back safely when it disappears. Region Map displays a text-backed danger recommendation/confirmation row plus Inspector-colored outline and cross only for verified explored-zone markers; fractional, unauthored or undiscovered marker injection is rejected. The shared AccessibilitySettingsMenu now scales Merchant, Blacksmith and Storage overlays alongside existing menus at 100/112.5/125% UI/text settings, with scene NodePaths and focused UI/accessibility tests. No new assets or artificial travel ability.
- **Verification:** Godot 4.6.2 stable Mono `tools/reference_validation.ps1` discovered **261 tests, 0 failures**; headless editor parse, main-scene smoke and `gl_compatibility` renderer-active reference smoke passed (`docs/evidence/reference-validation-latest.json`, generated `2026-09-20T04:00:20Z`). The recurring `Save rejected: invalid profile_id` stderr line remains an intentional negative fixture. Greybox performance data is not minimum-hardware/final production certification.

## 2026-09-20 code-only skills / combat / progression closure

This entry supersedes older missing-live-skill, unscoped-status and finite-route-XP descriptions below. PLAYTEST resources remain provisional; full production authoring, representative manual combat fairness and final numeric balance remain separate.

- **Nine active skills:** The shipped Q/R action owner retains Inspector-authored startup/commit/active/recovery/cooldown costs and live damage/ward delivery for the approved three actives per class. `PlayerPlaytestAttackDelivery` now gives Driving Thrust collision-solved committed forward motion, Backstep Shot collision-solved retreat with a 4-tick dodgeable-only evade window through `PlayerDefenderFactsProvider` (wired to both enemy and Tenth Warden defender snapshots), and Fan Shot three ordered angles at 2-tick intervals with unfired shots canceled on action interruption. Projectile travel stops at authored range or solid obstacles. The new Inspector PLAYTEST movement distances are 36 px Thrust / 64 px Backstep; these are hypotheses, not final tuning. `test_player_active_skill_distinct_effects.gd` and ten focused skill/loadout/ward/clock/defender tests passed. The reproducible generator `tools/create_player_playtest_attack_content.gd` includes the same new Inspector fields. Rank-2/3 magnitude/cost/recovery effects, passive runtime bonuses, final per-action authority and art/telegraph acceptance remain open.
- **DR-06 source identity:** Two Controllers no longer overwrite one another's Slow as one shared `status:playtest_slow`. `StatusPlaytestTuning.application(behavior, source_id)` can emit stable source-scoped Burn/Slow identities while unchanged one-argument callers preserve legacy IDs. The authenticated Controller active field passes caster identity, and source-aware Burn ticking preserves each source's cadence and strongest/reapplication rules. Existing dodge/block/parry/mitigation owners remain intact. Nine focused combat/status tests passed; final damage/duration numbers remain PLAYTEST.
- **Finite progression:** `progression_playtest_v01.tres` now provisionally awards **80 × floor number** XP on ten finite primary Quest Hall claims (4,400 XP total). The separately claimed Floor 10 boss reward remains 160 XP; completing the mandatory route therefore supplies 4,560 XP against the existing 4,500-XP Level-10 threshold without repeat farming or optional side quests. Floor 10 entry after nine primaries remains Level 9 at 3,600 XP; Level 10 clamps cumulative stored XP to 4,500, discards 60 overflow, grants exactly nine authored skill points/transitions, and never creates Level 11. Side A/B/C retain 40/60/80 independent once-only XP claims. `ProgressionPlaytestContent.finite_required_route_status()` reports the audited required-route total and fails content readiness if the finite route is underfunded; this does not invent new enemy/elite/exploration rewards. Planning, real Quest Hall Floors 1/4/10, durable save/rollback, cap and optional-after-cap tests passed. `tools/create_progression_playtest_content.gd` reproduces the 80×floor PLAYTEST table; existing saves with already-claimed XP retain their stored historical totals, with no invented retrospective payout/migration.
- **Broad verification:** `tools/reference_validation.ps1` on 2026-09-20 discovered **261 tests, 0 failures** in the latest superseding run, with editor parse, main-scene smoke and GL Compatibility renderer-active reference smoke green (`docs/evidence/reference-validation-latest.json`, generated `2026-09-20T04:00:20Z`). The recurring invalid-profile log is an intentional negative fixture; reference-greybox smoke is not minimum-hardware certification.

## 2026-09-20 code-only Region 3 / Summoner / Tenth Warden closure

This entry supersedes the older pending-runtime descriptions below; all new balancing, attack geometry, encounter counts and reward amounts remain explicitly PLAYTEST unless separately production-approved.

- **Region 3 R3-SIDE-A/B/C:** GameplayRoot now binds a live `Region3SideQuestRuntime` to the authored Region 3 quest markers, existing physical combat and Quest Hall. Side A uses a physical escort, its route/goal and three attributed hostiles; death/leave/retry and coherent escort HP/position snapshot restore are live. Side B owns five attributed combat targets and durable kill progress. Side C owns a damageable defense actor and three two-enemy waves; JSON restore of wave 2/3 respawns only surviving actor IDs, supports failure/retry through the safe-checkpoint path, and retains camera/foundation collision state. Finite Quest Hall XP claims award 40/60/80 once for A/B/C. Integral JSON floats for escort/defense HP are validated and normalized; invalid numbers still fail closed. Placeholder actors/markers are scene authored; final balance/art acceptance is separate.
- **Summoner:** Live authenticated ACTIVE-window reinforcement uses Inspector-authored finite two-call Duelist PLAYTEST tuning. Its shared Tower host reserves stable floor-wide spawn slots against population budgets, admits through existing combat/FULL-AI and movement/presentation owners, respects the global 12-agent limit, persists spent IDs in floor `quest_state`, excludes defeated actors on re-entry, and cleans up on encounter exit. This supersedes the old `reinforcement_budget_available=false`/deferred-Summoner description.
- **Tenth Warden:** Live Sanctum movement now approaches and lunges in locked windup aim, maintains arena margins, delivers directional shape-checked melee and three moving Arc Volley ColorRect projectiles, and exposes the provisional phase-two weak point only in heavy recovery. Punishing Step now derives its live rear/proximity condition from player position each physics tick, using Inspector-owned provisional 112 px range and -0.35 facing-dot threshold; normal action selection has a focused test. Phase transitions, dedup, death/retry teardown and no-Floor-11 behavior remain on their existing shared owners. Final manual readability/fairness approval and production tuning are still open.
- **Focused verification:** Godot 4.6.2 Mono headless editor parse and main-scene smoke passed; Region 3 live/restore/death-retry/escort/side-quest tests, Summoner budget/lifecycle tests and boss motion/projectile/autonomous/shipped/integration tests passed. Broad `tools/reference_validation.ps1` on 2026-09-20 discovered **260 tests, 0 failures**, and passed editor parse, main-scene and renderer-active reference smoke (`docs/evidence/reference-validation-latest.json`); subsequent skills/combat/progression changes are covered by the new closure entry above. The `Save rejected: invalid profile_id` message is the intentional negative fixture. Human playtest readability/fairness and final tuning approval remain separate gates.

## 2026-09-18 integrated closure snapshot

- **Player-facing manual save:** Pause exposes Save Game with truthful success/rejection feedback. Region 3 and Tower manual saves capture the current supported safe state and exact supported player position. Active Combat, death/retry and transient movement/action states reject manual save. A production Tower Escort permits manual save only when its exact actor position, HP, route index and wait state serialize coherently; unsupported Escort contexts without a restorable physical runtime still fail closed.
- **Autosave/checkpoint coverage:** new-profile Region 3 entry commits the authored town checkpoint; unlocked Tower entry commits a destination checkpoint; Floor 10 owns the authored pre-boss checkpoint; completed primary floors commit the authored exit checkpoint; returning from that exit to Region 3 immediately commits the required hub-entry autosave; accepted pending-reward claims request the shared autosave path. Floor-completion exit proof is stored durably in the floor state so Quest Hall turn-in remains strict even after the current safe snapshot advances to the hub.
- **Primary Tower progression:** live Floors 1–9 objectives can reach `ObjectivesComplete`, exit safely, return to Region 3, turn in at Quest Hall, mark the floor primary clear, and unlock exactly the next floor. Floor 10 turn-in marks the prototype milestone and never fabricates Floor 11. All ten primary definitions bind acceptance and turn-in to Quest Hall `r3:functional:02`. Floors 2–10 now own complete production-v01 quest contracts/objective allocation; successful prior-floor turn-in exposes the next primary as `Available`, and a separate explicit Quest Hall interaction accepts it into `Active`. `QuestCatalog.primary_production_status()` reports Floors 2–10 production-ready with no remaining quest/objective authority blockers. Floor 10 boss combat now runs through explicitly flagged PLAYTEST resources; final balance/readability approval remains separate.
- **Floor 10 boss boundary:** GameplayRoot owns the conditional production caller: after boss authoring, active Floor 10 objective and defender-facts readiness pass, it instantiates `TenthWardenSanctum`, reuses shared Active Combat/FULL-AI/attack-pressure ownership, binds the live authored contact-delivery driver and `Floor10BossProgressionBridge`, and routes terminal victory only into the existing objective/exit/Quest-Hall turn-in flow. The Twin Cut overlap regression is fixed at the boss delivery boundary. `PlayerDefenderFactsProvider` now owns the live defender snapshot path from authoritative player state: live ClassCombatRuntime block/parry mode, combat aim direction for frontal coverage, and a separately authored dodge-invulnerability interval; `PlayerDefenderFactsTuning` explicitly fails closed until dodge-window and frontal-coverage values are authored and assigned. The production integration test now exercises this normal GameplayRoot provider path rather than an injected defender callback. Shipped boss-room entry now succeeds with newly authored and inspector-assigned, machine-flagged PLAYTEST resources: `tenth_warden_playtest_v01.tres` has provisional HP/stamina/regen/defense/poise, explicit block/parry/interrupt declarations, both phase timings and costs for all five moves, live shape-overlap geometry/hit intervals, payloads and heavy-recovery weak-point cadence; `player_defender_facts_playtest_v01.tres` specifies a 0.015–0.105-second dodge invulnerability interval within the authored 0.12-second dodge and 120-degree frontal defense coverage. `tools/create_tenth_warden_playtest_tuning.gd` reproduces these editable `.tres` values; `get_floor10_boss_production_status()` reports `playtest_placeholder = true`. A native ColorRect replaces the *visible* boss sprite while retaining the existing semantic Sprite2D/AnimationPlayer presentation boundary; Phase 2 and weak-point exposure receive distinct Inspector-owned placeholder colors without generating images. Deterministic stamina recovery avoids permanent boss action starvation; Sanctum exit tears down combat resources on unload. The authoritative master did not supply final numeric balancing, so these numbers/shapes are not final; provisional Arc Volley now has per-hit-index traveling shape-checked ColorRect projectile entities; final motion/aim/telegraph alignment and playtest fairness are still pending, and final motion/aim/telegraph readability remains open. No boss reward or Floor 11 is fabricated.
- **Supported status / active-skill production:** `ClassCombatRuntime` serializes normalized supported status states; `PlayerCombatantRuntimeBinding` restores them into new encounter state and hands exact remaining status ticks back to the safe-state owner before normal encounter teardown. Burn/Slow HUD rows read the authoritative live runtime. `ActiveSkillProductionAuthority` has aligned records for all nine approved active skills and `SkillExecutionService` has a strict production request path, but every active still reports explicit missing action/payload/geometry/timing values and therefore no final production skill-authority approval is implied. The shipped scene now assigns flagged `active_skills_playtest_v01.tres` with nine provisional action clocks, and `status_effects_playtest_v01.tres` with 180-tick Burn (2 unmitigated HP every 30 ticks, including expiration) and 150-tick Slow (25% movement reduction, strongest-only, 40% minimum enemy speed). Tower encounter ticks apply playtest Burn HP/defeat and enemy movement consumes the Slow multiplier; `status_damage_resolved` now mirrors live Burn HP (including fatal ticks) into the player HealthComponent before the defeat event, synchronizing overlapping player encounter states without double-resolving contact. `test_player_burn_live_health_mirror.gd` covers tick cadence, live HP, overlapping encounters, fatal event ordering, no repeated death, and disconnected binding cleanup. The player consumes the same optional Slow tuning for walk, run and dash velocity using live encounter statuses or restored ClassCombatRuntime status state. Status clearance restores normal speed without modifying action/dodge clocks. `test_player_status_slow_playtest.gd` verifies player speed, strongest-only clamping, burst displacement and expiration. The shipped Q/R active-skill path now executes the Inspector-assigned startup/commit/active/recovery/cooldown clocks with the real player stamina or mana pool. `PlayerPlaytestAttackDelivery` handles provisional melee envelopes, traveling ranged/mage projectiles, delayed pulses and the temporary Aegis Ward block; Aegis lasts the authored 90 ticks, uses shared defense facts for enemy/boss contacts, never enables normal hold-block or parry and does not persist as mage shield equipment. These effects remain explicitly provisional rather than production-accepted. Final production skill authority, exact motion/aim and balancing remain gated.
- **Playtest progression and per-archetype combat:** `progression_playtest_v01.tres` is inspector-assigned with cumulative XP, discard-at-Level-10, nine HP/stamina growth and skill-point entries, class baselines, and separate unique XP claim/source definitions for ten primary turns-ins, side A/B/C and boss defeat. The provisional ten primary Quest Hall turn-ins now pay their Inspector-authored XP automatically through `PlaytestQuestXpCommitService` after the authoritative quest clear is saved; completed Region 3 side quests can use the same gated claim path once their live quest runtime exists. The dedicated claim, HP/stamina growth and skill points are staged and saved together with a live stat mirror that preserves current injury/exertion; failed XP saves roll back the mirror and leave the already-completed quest retryable on another Quest Hall interaction. `test_gameplay_quest_xp_playtest.gd` covers JSON save/load, duplicate claim, real level-up, failed save/rollback and retry. R3-SIDE-A/B/C playtest XP sources now use their actual `QuestCatalog` side quest IDs rather than invented slot names. Floor 10 Quest Hall now makes the separate boss-completion XP claim only after persisted boss defeat and durable primary clear, with independent claim ID and retry behavior; this provisional system does not imply final production XP approval. `enemy_attacks_playtest_v01.tres` provides provisional DR-06 shapes and payloads for nine damaging archetypes and attaches to live Tower residents; Support temporary ally ward and Controller observed-position Slow field now execute through authenticated active windows with geometric marker scenes; Summoner still rejects without a budgeted live reinforcement owner. Shipped enemy damaging signatures now have live, authenticated contact/projectile delivery, but final attack geometry, aim fairness and balance remain provisional.
- **Provisional equipment combat effect:** `player_equipment_playtest_v01.tres` is assigned on the shipped `ClassCombatRuntime` Inspector. Equipped starter sword/bow/staff `upgrade_rank` now contributes the provisional `attack_power` delta to real basic and damaging skill payloads, and Blacksmith equipment upgrades refresh that live binding immediately. The bonus vanishes on unequip; Aegis Ward, bare hands, other categories and unauthored armor/accessory benefits do not acquire invented stats. `test_gameplay_equipment_attack_playtest.gd` and `test_gameplay_aegis_ward_playtest.gd` verify Inspector tuning, actual payloads, block-window lifecycle and safe expiration.
- **Inspector/UI warning cleanup:** `tools/audit_ui_layout.py` checks every project `.tscn` for autowrapped Labels in Containers/ScrollContainers without a nonzero custom minimum size; 28 affected Label nodes (including Quest Log Detail) now own `Vector2(1, 1)` minimums in their scenes, with no scripting workaround. The two status-row labels expand horizontally. Inventory normal-slot buttons and Tower floor buttons instantiate Inspector-assigned PackedScene templates instead of hardcoding Button construction, sizing, focus and alignment; Tenth Warden's ColorRect path and three provisional phase/weak-point colors are Inspector-owned. Authorized gameplay caps, semantics, timings, claims and dynamic text remain in code/resources. Dedicated menu/boss UI tests and editor parse cover the refactor.
- **Playtest consumable input and healing:** The shipped `consumables_playtest_v01.tres` owns temporary tonic healing (25 HP) in an Inspector-editable, machine-flagged resource. Existing 1–4 shortcuts now route through `ConsumablePlaytestUseService`: an authored consumable must be owned in the exact quick-slot instance; at non-full live HP it consumes exactly one normal-stack unit, mirrors recovery through `HealthComponent`, leaves partial-stack references intact, clears all references on stack exhaustion, and records a unique transaction claim. Active modal, held-key suppression, death, stale/missing category authority, full HP, and unauthored material references fail closed without consumption. Mutations remain explicitly unbanked until the next coherent safe snapshot; no combat save bypass is invented. `test_gameplay_consumable_playtest.gd` proves live HP, quantity/claim, shared references, full/empty/unauthored rejections, modal gating and configured healing.
- **Region 3 live services:** Storage, Quest Hall, Training Hall, General Merchant, Blacksmith, Inn and Clinic requests are admitted through the same physical service/guard path and mark exact service discovery. Training Hall remains the only Skills mutation owner. General Merchant can initialize once from an explicitly assigned valid `VendorStockCatalog`, then uses persisted vendor state; Blacksmith opens and upgrades only from an explicitly assigned valid `UpgradeRecipeCatalog`; the shipped Gameplay scene now assigns explicit `region3_services_playtest_v01.tres` stock/prices and three starter-weapon upgrade recipes; final production economy approval remains open. Focused live fixture tests verify exact merchant price/stock consumption and Blacksmith Gold/material/rank/provenance behavior without inventing production content.
- **Inn/Clinic recovery integration:** `Region3RecoveryServiceDefinition` still requires explicit role/availability/combat/price/recovery/stock/save-event data. Gameplay now additionally requires an explicit authored-resource → runtime-resource mapping before direct recovery can run. A valid Inn fixture atomically commits Gold deduction + transaction claim + mapped HP/stamina recovery + Region 3 safe-state save and rolls live resources back if durable save fails; Active Combat blocks the service. Stock-only Clinic content remains outside direct-healing ownership. The shipped scene now assigns playtest Inn (8 Gold, 35 HP/20 stamina) and Clinic (15 Gold, 60 HP/30 stamina) definitions with stable mapping and persisted Clinic stock; its combined merchant surface offers a user-visible Recover button with the existing atomic/save guard. Exact balance remains provisional.
- **Inventory quick slots:** all four slots are consumable references. New binding requires explicit `ItemCategoryCatalog` authority proving the item definition is `consumable`; missing authority, unauthored categories and non-consumables reject. Legacy persisted references remain load-compatible and are surfaced as unvalidated when category authority is unavailable.
- **Physical Tower Escort runtime:** the production Tower Escort path is authored and live for Floors 2/6/8: `CharacterBody2D` actor, deterministic validated route, 80 px/s movement, 2 px arrival tolerance, 12×10 collision footprint, layer/mask 1/1, 100 HP, wait/follow state, collision feedback/retry-on-route, actor-defeat failure and safe retry. Gameplay assigns `tower_escort_runtime_production_v01.tres` with the existing authored 100 HP and exact health/failure/separation/repath/save policy IDs; `test_tower_escort_runtime.gd` now validates the assigned production resource directly so missing fields fail at the focused gate. Manual save/load preserves exact actor position, HP, route index and wait state. The runtime accepts already-resolved HP damage and does not introduce a second mitigation formula.
- **Region 3 world/map data and side-quest staging:** `Region3WorldLayoutDefinition` is assigned to the authored town scene and validates the master-backed R3-TOWN/OUTSKIRTS/ROADS/RUINS/RISK zone reservations plus stable side-quest anchor identities. `ProfileSnapshot.region_state` is now the backward-compatible durable owner for the Region 3 map revision and explored zone IDs. `Region3SubzoneDiscoveryService` maps the live player's physical tile only to those existing authored rectangles; GameplayRoot records discovery on actual zone entry with a zone-change cache, and the initial R3-TOWN discovery is committed in the existing Region 3 starting checkpoint save. Read-only map inspection never initializes discovery, unknown zone IDs reject, and a persisted map-revision mismatch fails closed instead of silently resetting exploration. `Region3MapSnapshotService`, `MapViewService`, `MapMenu` and `MapRegionCanvas` expose/render only explored zone rectangles plus exact zone IDs; later discoveries remain live-but-unbanked until a legitimate coherent save. Exact quest-marker tiles, Region checkpoint coordinates and one concrete Recommended Level per hostile subzone remain deliberately unavailable with explicit reason IDs. Source-backed staging boundaries still exist for R3-SIDE-A Escort south outskirts, R3-SIDE-B Annihilation west road/field and R3-SIDE-C Defense north quest site without silently enabling unauthored actor/goal anchors. `region3_side_quests_playtest_v01.tres` now contains explicitly provisional A/B/C wave counts (3 / 5 / 2+2+2), retry/leave IDs and matching finite XP definitions, but each slot deliberately has `runtime_enabled=false`: real escort/defense actor anchors, spawns and quest execution remain unimplemented.
- **Quest authored-contract validation:** every `QuestDefinition` declares/validates prerequisite, acceptance anchor, turn-in anchor, required/optional objective IDs, instance bindings, leave/failure/retry, reward, branch and completion-condition fields. `validate_production_contract()` rejects any `playtest_placeholder`, and `QuestActivationService.activate_authored()` requires the exact declared anchor/prerequisite facts. Floors 2–10 now use the authored production-v01 contract and objective allocation, report production-ready with empty blocker sets, and remain explicitly bounded to Floor 10 with no Floor 11 identity. Structural catalog validation remains separate from production readiness.
- **Audio:** native `AudioStreamPlayer`/`AudioStreamPlayer2D` playback and lifecycle cleanup are green. Gameplay music routing is integrated conditionally: exploration/combat follow shared Active Combat, boss/recovery require explicit override callers, and teardown stops persistent ownership. The integration resolves `/root/AudioService` at runtime so standalone gameplay tests compile cleanly. No production `AudioMusicRoutingDefinition` with final streams is assigned, so production music remains fail-closed.
- **QA-PERF-WORST:** the latest current-tree quick protocol successfully release-exported `build/qa_perf/nice-journey-qa-perf.exe` and ran the deterministic 1280×720 Floor 9 fixture on the developer i5-1135G7 / Iris Xe machine. Measured 2.019 s sample: p50 **16.673 ms**, p95 19.968 ms, p99 22.544 ms, max 24.639 ms, 0% frames >40 ms, rolling one-second minimum 59 FPS. The quick sample therefore misses the reference p50 threshold by 0.003 ms; this result is retained rather than relaxed or rerun for a preferred pass. Route completeness is false because representative projectile entities, authored audio-stream mix and authored Tenth Warden timing are absent. Evidence records `final_hardware_acceptance=false` and `certification_blocked=true`; the full 60 s + 10 min exported protocol and approved i5-8250U/UHD-620-equivalent physical-hardware run remain required after representative content exists.
- **Region 3 Batch 2 art:** the provided source ZIP is truncated/corrupt and the seven exact source PNGs are absent. Existing V02 preview derivatives remain unaccepted and are not provenance evidence.
- **Current broad integration gate:** `docs/evidence/reference-validation-latest.json` reports **246 dynamically discovered tests, 0 failures**; the current full suite includes the player-side Slow movement regression, with headless editor parse, configured main-scene smoke and renderer-active reference smoke green on Godot 4.6.2 Mono `gl_compatibility`. The current reference-machine frame/memory measurements are recorded in that evidence file; they remain reference-greybox evidence only, not DR-08 minimum-hardware/final acceptance. The recurring `Save rejected: invalid profile_id` stderr line is the intentional negative save fixture, not a gate failure.

## Implemented

### M0 — contract/data baseline

- Rebindable runtime InputMap defaults for the approved prototype actions.
- Stable ID validation utility.
- Minimal typed profile snapshot with schema versioning.
- Three-slot save service boundary.
- Atomic temporary-write/verify/commit flow.
- Previous-valid-generation backup recovery.
- Detailed save-load status distinguishes missing/corrupt/incompatible generations, reports the actual current and backup generation status, selects a valid current generation before prior-valid backup fallback, never fabricates a profile when no valid generation exists, and preserves the legacy `load_profile()` behavior.
- Minimal validation for protagonist name, class ID, level cap, XP, skill points and persistent flags.
- Persistent one-shot claim transaction dictionary is part of the profile snapshot and survives atomic save/load.

Executable gates: `tests/test_m0.gd` and `tests/test_save_compatibility.gd` — **PASS**.

### M1 — movement/presentation greybox

- Main runnable greybox scene.
- Rectangle lower-body player collision.
- Normalized WASD velocity calculation.
- Movement tuning is stored in `MovementTuning` Resource data rather than hard-coded gameplay values.
- Distinct dash and dodge burst states.
- Stamina capacity/recovery tuning is stored in `StaminaTuning` Resource data.
- Stamina spend/regeneration component.
- Blocked burst movement resolves through CharacterBody2D collision instead of tunneling through the test obstacle.
- Side-only body facing with vertical hysteresis/dead-zone behavior.
- Explicit player ground anchor and separate mirrored grip anchor; lower-body collision remains authoritative and unmirrored.
- Independent continuous weapon/aim pivot sourced from the mirrored grip transform.
- Local behind/front weapon ordering based on aim direction uses sibling ordering at `z_index = 0`, so the weapon cannot globally outrank unrelated foreground geometry.
- Gameplay world Y-sorting uses ground/bottom anchors; the greybox foreground block was converted from center origin to bottom origin without changing its collision footprint.
- Pixel-safe authored camera zoom steps at 1× and 2×.
- Capped camera look-ahead.
- Explicit camera follow center with a small screen-space dead zone and subtle exponential smoothing; the final presentation center is clamped after follow, look-ahead and shake are combined.
- Bounded camera shake with reduced-motion suppression; teleport/climb traversal clears interpolation/shake history.
- Foreground-wall occlusion fades only the obscuring upper visual while preserving the opaque base and authoritative collision.
- Reusable authored `ClimbLink` interaction: Interact/E can traverse only while the player is inside a compatible link, normal movement/bursts are suppressed during the short transition, and the lower-body collision footprint must fit the declared landing before the snap commits.
- Invalid/missing/blocked climb destinations leave the player at the source rather than bypassing collision.
- Temporary greybox visuals only; no final art was created.

Executable gate: `tests/test_m1.gd` — **PASS**.

Automated M1 evidence now covers movement normalization/collision, side-facing/continuous aim, grip/body local ordering, world Y-sort setup, **actual runtime visible-rectangle bounds at 1×/2×** while follow/look-ahead/shake are active, capped look-ahead, screen-space dead-zone conversion, viewport↔world aim-coordinate round trips at clamped boundaries, widened-viewport bound math, bounded/reduced-motion shake, foreground occlusion and local weapon ordering at both zoom steps with collision preserved, authored climb-link admission, modal blocking, landing validation at both start and completion, overlapping-link retention and camera reset only on committed traversal. Remaining presentation acceptance is visual: additional representative resize/aspect coverage, representative raster aim silhouettes/grip attachment, and final-art approval. The current greybox occluder proves player reveal/collision preservation, but objective reveal and “no undiscovered-room leak” cannot be closed until objective/discovery-room state exists.

Renderer-active provisional screenshots now exist under `docs/evidence/visual/` for the actual running profile shell and gameplay greybox, documented in `docs/VISUAL_EVIDENCE_PROVISIONAL.md`. Dedicated PNG/JSON evidence verifies the minimum supported output at **1280×720** and separately verifies the recommended **640×360 internal canvas**; these must not be conflated. The max-current-scale settings capture records the fixed horizontal-overflow regression. A separately captured real 1920×800 physical-client top-level Godot window shows the rendered game surface remains exactly **1280×720** with black surrounding bars, so that ultrawide case does not widen the greybox world view or expose additional off-room area. The focus-loss pause overlay limits that evidence to composition/letterboxing/world-view extent. None of this is final pixel-art/telegraph/accessibility acceptance, and undiscovered-room leak behavior remains untestable until discovery-room/objective state exists.

### M2-neutral foundation

This is framework work only, not completion of M2.

- Typed `ActionDefinition` resource.
- Fixed-tick `Startup → Commit → Active → Recovery` state machine.
- Explicit action-instance IDs.
- Commit-point event emitted once.
- Cooldown begins at commitment.
- One replaceable buffered intent with gameplay-tick expiry.
- Busy actions are not silently cancelled by a new request.
- Invalid action data is rejected before admission.
- Aim-using actions declare a Startup/Commit/Active aim-lock point and whether tracking after that point is explicitly authored; the state machine emits the lock boundary once per action instance.
- Each aimed action instance owns its request/current aim sample; the exact sample at the declared lock boundary is retained independently for that instance, buffered actions preserve their own sample, and post-lock retargeting is rejected unless tracking is explicitly authored.
- Generic forced interruption has a distinct terminal signal with an explicit reason ID; it leaves the interrupted action idle without also emitting normal completion or selecting any damage/poise rule.
- Action phase timing, buffered intent/expiry, and currently permitted authored cancel IDs are exposed for development observability without binding any class kit.
- Generic resource pools provide explicit resource ownership without assuming a class kit.
- Cost resource/amount is declared in action data.
- Startup checks eligibility without spending; commitment revalidates and spends exactly once.
- Failed commitment revalidation cancels before active frames and does not start cooldown or partially spend.
- Class-specific cost rules and damage/defense semantics are deliberately not chosen.

Executable gate: `tests/test_m2_foundation.gd` — **PASS**.

### M2-neutral contact identity / duplicate admission

- `CombatContactLedger` records contact identity only; it does not resolve damage, HP, defense, crits, weak points, status, poise or class behavior.
- Contact admission is keyed by action-instance ID + stable target ID + authored hit-interval/index.
- The first contact for a key is admitted and an identical repeat is rejected; distinct authored intervals, targets and action instances remain independent.
- Contact records can be cleared for one completed action instance without erasing another instance's records.

Executable gate: `tests/test_combat_contact_ledger.gd` — **PASS**.

### M2-neutral action-intent / modal integration

- Gameplay now composes the neutral `ActionStateMachine` behind an `ActionIntentController` that uses the existing shared `InputOwnership` as its only UI/gameplay admission authority.
- Opening or closing a modal clears the one buffered combat/use intent without cancelling the already-active action; a modal-owned input cannot create a new action intent.
- The input that closes a modal remains blocked at the action-intent boundary until its matching release, while unrelated gameplay actions remain independently routable.
- The live action machine remains scene-driven by the existing physics tick; `SceneTree` pause stops its ticks and unpausing resumes them. No second combat clock was introduced.
- A development snapshot exposes the current action/instance, readable phase and tick age, buffered intent/expiry, observed cooldown, allowed cancels, aim-lock/sample state, current modal and last input-admission rejection. Attack/target IDs and DR-06 damage/defense results are still **not implemented in this snapshot path** until a real contact-resolution owner exists; the governing DR-06 semantics themselves are now resolved in master §58.

Executable gate: `tests/test_action_input_integration.gd` — **PASS**.

### M3-neutral fair-observation / memory foundation

This is isolated fair-AI data-contract work only, not production enemy AI or completion of M3.

- `AiObservationAdmission` is a fixture/data-only admission boundary with explicit sight, sound and shared-fact methods. It accepts only stable source/state identities, finite position/confidence, non-future observation timestamps and still-live facts, then writes the normalized canonical cue into `AiObservationMemory`.
- Shared-fact delivery never refreshes the original observation timestamp or confidence. Delivery/admission time is used only to reject facts that are already expired; accepted facts keep their original observation age/uncertainty.
- `AiObservationMemory` stores only observed facts: cue/source identity, observed position/state, observation tick, confidence, authored lifetime and derived expiry.
- Memory is bounded; capacity eviction removes the oldest fact by observation timestamp rather than arrival order, so a late older shared fact cannot displace newer knowledge.
- Per-source reads return the newest still-valid observed fact with explicit age and caller-authored confidence decay. Returned snapshots are detached copies, so debug/read consumers cannot mutate stored facts.
- Last-known position/state remain the values actually observed; the contract exposes no raw player input and no live target handle, so it cannot provide unseen live tracking by itself.
- Reaction eligibility cannot become true before `observed_tick + authored_reaction_delay_ticks` and expired facts cannot authorize a delayed reaction.
- Malformed/non-finite/future/expired-at-admission facts are rejected without mutating memory; non-finite confidence decay is also rejected on reads.
- Memory purge/reset is explicit and deliberately does not choose an encounter reset, death, replay, tactic, attack, utility or group-coordination policy.

Executable gates: `tests/test_ai_observation_admission.gd` and `tests/test_ai_observation_contract.gd` — **PASS**.

### M3-neutral FULL-AI simulation admission — §23/§23.1

- `FullAiSimulationLedger` is a pure data boundary for the locked exceptional hard cap of 12 simultaneous FULL-AI combatants across overlapping encounters.
- Admission requires stable actor and encounter IDs. One actor can hold at most one admission, so duplicate requests cannot double-count or silently move the actor between encounters.
- Capacity is global to the ledger rather than per encounter: actors from overlapping encounter IDs share the same 12-slot maximum, and a 13th admission is rejected without eviction or replacement policy.
- Release is explicit by actor ID; repeated or unknown releases are safe no-ops and cannot under-count active admissions. Released capacity is reusable by a later valid admission.
- Read-only debug state exposes only the locked cap, current count and deterministically actor-sorted actor/encounter identity pairs. Returned data is detached from ledger state.
- The ledger does not choose lifecycle transitions, dormant/reduced-simulation behavior, scheduling, freezing, priority, tactics, attack/pressure budgets, encounter content, cooldown resets, or DR-01/02 retry/restoration semantics. It is not wired to fake runtime enemies.

Executable gate: `tests/test_full_ai_simulation_ledger.gd` — **PASS**. `tests/test_enemy_root_lifecycle.gd` remains green as the adjacent lifecycle regression.

### M3-neutral Active Combat state boundary — §23.1

- `ActiveCombatRegistry` is a pure/composable state boundary for whether combat is active; it does not own operation blocking or call `GameplayOperationGuard`.
- Acquisition uses stable source IDs plus one of the four locked causal reason IDs: engaged hostile encounter, unresolved attack threat, contested encounter objective, or unresolved boss phase/committed attack.
- Each source/reason pair is unique and duplicate-safe. Release is explicit by pair or source, and clearing one pair/source cannot clear unrelated active causes owned elsewhere.
- Combat becomes inactive only when the final explicit cause is released. The registry has no distance/LOS auto-clear, grace-duration tuning, encounter-end inference, music behavior, tactics, damage, save trigger, or DR-01/02 retention/restoration policy.
- Debug state exposes only the current active flag, cause count and deterministic source/reason pairs sorted by stable IDs. Returned debug data is detached from registry state.

Executable gate: `tests/test_active_combat_registry.gd` — **PASS**. `tests/test_operation_guard.gd`, `tests/test_full_ai_simulation_ledger.gd` and `tests/test_enemy_root_lifecycle.gd` remain green as adjacent boundary regressions.

### M3-neutral attack reservation identity/lifecycle — §22.7

- `AttackReservationLedger` is a pure coordination boundary for reservation identity before action commitment; it is not wired to production enemies yet.
- Admission requires stable actor, target and encounter IDs plus a positive action-instance ID. One actor/action-instance identity can hold at most one reservation, so duplicate requests cannot double-count or silently remap target/encounter ownership.
- Reservation acquisition is one atomic `try_reserve()` mutation boundary returning a unique monotonic token. Future runtime integration must call this before the real action commitment boundary; no fake runtime owner was added merely to claim wiring.
- Individual release accepts only the exact locked §22.7 causes: **completion, cancellation, interruption, death, target invalidation, or encounter exit**.
- Bulk cleanup is deliberately narrower: actor cleanup is death-only, target cleanup is target-invalidation-only, and encounter cleanup is encounter-exit-only. Clearing one scope cannot erase unrelated reservations.
- Debug state is bounded, deterministic and detached. The ledger chooses no attack selection, tactics/utility, pressure limit, damage, cooldown, admission schedule, or DR-01/02 replay/death-restoration semantics.

Executable gate: `tests/test_attack_reservation_ledger.gd` — **PASS**. Independent QA also reran editor parse plus `tests/test_enemy_root_lifecycle.gd`, `tests/test_full_ai_simulation_ledger.gd` and `tests/test_active_combat_registry.gd` — **PASS**.

### M3-neutral attack-participation admission — §22.7 / §23.1

- `AttackParticipationAdmission` composes the existing lifecycle, FULL-AI and reservation boundaries without becoming a runtime encounter owner.
- An attack reservation may be delegated only when the supplied `EnemyRootLifecycle` is currently reservation-eligible (Active) and the same stable actor ID is already admitted to FULL-AI simulation for the exact same encounter ID.
- `FullAiSimulationLedger.is_admitted_to_encounter()` is a read-only exact actor/encounter membership query; it does not change admission, capacity, scheduling, priority or release behavior.
- Reservation identity, target/action-instance validation, duplicate rejection and remap rejection remain owned by `AttackReservationLedger`; the composition gate does not duplicate or weaken those rules.
- Dormant, Defeated, Returning and Resolved lifecycle states, Active actors without FULL-AI membership, and encounter-mismatched actors are rejected before reservation mutation. Rejected admission does not change lifecycle state/generation, FULL-AI accounting or reservation accounting.
- The composition chooses no tactics, utility/attack selection, pressure budget/attack-count limit, admission schedule, damage, cooldown, automatic cleanup, replay/death restoration or fake scene ownership.

Executable gate: `tests/test_attack_participation_admission.gd` — **PASS**. Independent QA reran editor parse plus `tests/test_enemy_root_lifecycle.gd`, `tests/test_full_ai_simulation_ledger.gd`, `tests/test_attack_reservation_ledger.gd` and `tests/test_active_combat_registry.gd` — **PASS** with no defect found.

### Region 3 town-structure manifest validation — §24.4 / §61 QA-REGION

- `Region3TownStructureManifestValidator` is a pure validation boundary for caller-supplied Region 3 town structure entries; it is **not** an authored production town manifest or layout.
- A valid manifest requires exactly **20** unique stable structure IDs, exactly **8 functional** entries and exactly **12 decorative** entries.
- The eight functional entries must cover each locked prototype role exactly once using transparent machine IDs: Central Tower, Quest Hall, Blacksmith, General Merchant, Inn / Rest House, Storage House, Training Hall and Clinic / Apothecary.
- Decorative entries require only a unique stable structure ID plus the decorative category. No decorative subtype identities, building names or coordinates are fabricated by the validator.
- Validation rejects malformed entry/field types, malformed or duplicate structure IDs, unknown/missing/duplicate functional roles, wrong total counts and either direction of an incorrect functional/decorative split. Error ordering is deterministic and caller input is not mutated.
- The slice deliberately does **not** encode exact coordinates, lot/plaza geometry, the tunable ~160×160 Region 3 size target as canon, quest anchors, service behavior, enterability runtime, travel rules, art, other-region content, or DR-03 quest/Sigil packaging.

Executable gate: `tests/test_region3_town_structure_manifest.gd` — **PASS**. Independent QA also reran editor parse and `tests/test_project_resource_paths.gd` — **PASS** with no defect found.

### Stable world-region catalog validation — §24.1–§24.7 / §61 QA-MAPS

- `WorldRegionCatalogValidator` is a pure validator over caller-supplied world-region catalog entries; it does **not** create authored world-map geography, travel data or other-region gameplay content.
- A valid catalog requires exactly **12** entries with integer `region_id` values **1 through 12 exactly once**. Numeric region IDs are the stable identity; placeholder/presentation names are optional and do not define identity or require the current placeholder spellings.
- Region **3** alone must carry the transparent prototype state `prototype_starting_region`; Regions **1–2 and 4–12** must carry `future_locked`. Empty/missing/wrong-type states and any non-3 playable/start/unlocked assignment are rejected.
- Validation rejects malformed top-level/entry/field types, non-integer/out-of-range/duplicate/missing region IDs and malformed optional name types deterministically without mutating caller input.
- The slice deliberately does **not** encode coordinates/boundaries/layout, the tunable Region 3 size target, discovery/exploration state, quest/travel eligibility or methods, service/quest/checkpoint/Danger markers, Tower Floor Map data, UI presentation, art/lore, other-region gameplay, or DR-03 quest/Sigil packaging.

Executable gate: `tests/test_world_region_catalog.gd` — **PASS**. Independent QA also reran editor parse and `tests/test_project_resource_paths.gd` — **PASS** after the empty-state rejection regression, with no remaining defect found.

### Map-layer identity separation — §24.6 / §61 QA-MAPS

- `MapLayerIdentityValidator` is a pure validator for the locked map-layer identity split; it does **not** create production map data or runtime map navigation.
- A valid caller-supplied descriptor set contains exactly three unique machine layer IDs exactly once each: `world_map`, `region_map`, and `tower_floor_map`. Descriptor order is irrelevant.
- Optional `name` presentation data may be absent or any `String`/`StringName` and does not define layer identity.
- Validation deterministically rejects malformed top-level/entry/field types, wrong 2/4-entry totals, missing/unknown/duplicate layer IDs and malformed optional names without mutating caller input.
- The slice deliberately does **not** encode coordinates/boundaries/layout, discovery/exploration, quest/travel eligibility, markers/services/checkpoints/Danger, floor generation/content, minimap/UI behavior, art/lore, other-region gameplay or DR-03 semantics.

Executable gate: `tests/test_map_layer_identity.gd` — **PASS**. Independent QA reran editor parse plus `tests/test_project_resource_paths.gd` — **PASS** with no defect found; this proves identity/separation only, not full QA-MAPS navigation or content behavior.

### Prototype tower-floor catalog identity — §4.1 / §4.5

- `PrototypeTowerFloorCatalogValidator` is a pure validator for the locked prototype Floor 1–10 identity set; it does **not** create production floor data, generation behavior or runtime tower progression.
- A valid caller-supplied catalog contains exactly **10** entries with integer `floor_id` values **1 through 10 exactly once**, in any order.
- Optional `name` presentation data may be absent or any `String`/`StringName` and does not define floor identity.
- Validation deterministically rejects malformed top-level/entry/field types, 9/11-entry totals, missing/duplicate/out-of-range/non-integer floor IDs and malformed optional name types without mutating caller input.
- The slice deliberately does **not** encode unlock/teleport/Danger/recommended-level policy, quests or DR-03 packaging, generation seeds/layout/modules, enemies/elites/boss/DR-07, replay/reset/reward/DR-01/02, map UI/navigation ownership, Floor 11+ content, lore or art.

Executable gate: `tests/test_prototype_tower_floor_catalog.gd` — **PASS**. Independent QA reran editor parse plus `tests/test_project_resource_paths.gd` — **PASS** with no defect found; DR-01/02/03/07 remain untouched.

### Equipment-slot identity validation — §20.1 / §20.6

- `EquipmentSlotIdentityValidator` is a pure validator for the locked prototype equipment-slot identity surface; it does **not** create production equipment, item instances or equip behavior.
- A valid caller-supplied descriptor set contains exactly five unique machine IDs exactly once each, in any order: `weapon`, `armor`, `off_hand`, `accessory_1`, and `accessory_2`.
- Optional `name` presentation data may be absent or any `String`/`StringName` and does not define slot identity.
- Validation deterministically rejects malformed top-level/entry/field types, 4/6-entry totals, missing/unknown/duplicate IDs and malformed optional name types without mutating caller input. The two accessory identities remain distinct.
- The slice deliberately does **not** encode item-instance/rarity/affix schemas, inventory/storage mutation, equip swaps/transactions, two-handed/off-hand compatibility or starter-kit/resource policy under DR-04, stats/damage/defense under DR-06, upgrades, visuals/UI, save/loot/economy behavior or runtime wiring.

Executable gate: `tests/test_equipment_slot_identity.gd` — **PASS**. Independent QA reran editor parse plus `tests/test_project_resource_paths.gd` — **PASS** with no defect found; DR-04/06 remain untouched.

### Interaction commit admission — §25.1

- `InteractionCommitAdmission` is a caller-driven commit boundary for the locked §25.1 revalidation rule; it does **not** choose an interaction target or perform the interaction effect.
- The caller supplies one stable logical `commit_id` plus already-resolved boolean facts for range, target-state validity, required-item satisfaction, required-quest-flag satisfaction, menu ownership, combat ownership and required-hold completion.
- Admission succeeds exactly once only when every supplied commit-time fact is true. The same admitted logical commit is rejected on repeat input/duplicate animation delivery before a caller may perform the effect.
- A rejected attempt does not consume its commit identity, so the same logical commit may succeed later after the caller revalidates and corrects the blocking condition. Malformed IDs/contexts reject deterministically and validation does not mutate caller input.
- The slice deliberately does **not** implement target arbitration/ranking, aim/distance/occlusion calculation or range tuning, quest/item definitions or consumption, shop/chest/dialogue/service effects, hold-duration policy, UI prompts, runtime scene wiring or any DR-01..DR-08 outcome.

Executable gate: `tests/test_interaction_commit_admission.gd` — **PASS**. Prime reran Godot editor parse plus `tests/test_project_resource_paths.gd` — **PASS**; repository-wide Wave 20 evidence is recorded below after the durable state text was reconciled.

### Protected quest/key-item operation policy — §26.1 / §26.2

- `ProtectedItemOperationPolicy` is a pure protection boundary for caller-resolved item protection facts; it does **not** own inventory state or execute item operations.
- Supported protection checks are `drop`, `destroy`, `sell`, and `quest_remove`. Normal/unprotected items are not blocked by this protection layer, but other owners may still reject an operation for ownership, capacity, confirmation, price, quest state, or other valid reasons.
- Critical quest/key items reject `drop`, `destroy`, and `sell`; ordinary quest logic may remove them through `quest_remove`.
- The permanent Tower Sigil rejects **all four** operations, including ordinary `quest_remove`, preserving the locked permanent-entitlement rule even if the caller omits the generic protected flag.
- Malformed operation/protection inputs reject deterministically and evaluation does not mutate caller state.
- This slice deliberately does **not** define item/stack schemas, stack compatibility, inventory/storage mutation, normal-grid capacity allocation, reward overflow, quest packaging, save/retry/death semantics, shop transactions, destroy-confirmation UI, runtime wiring, or any DR-01..DR-08 outcome.

Executable gate: `tests/test_protected_item_operation_policy.gd` — **PASS**. Independent QA found no defect and explicitly confirmed this is a protection-only boundary, not full inventory-operation admission. Prime editor parse and `tests/test_project_resource_paths.gd` also **PASS**.

### Normal-inventory capacity policy — §26 / §26.2

- `NormalInventoryCapacityPolicy` is a pure caller-driven capacity boundary for the locked **16 normal item/stack slots**; it does **not** own inventory contents or perform pickup/reward transactions.
- The caller supplies the current occupied normal-slot count, whether the incoming ownership is protected quest/key ownership, and whether the caller has already resolved that a new normal slot is required.
- Occupied normal-slot counts must be integers in **0..16**. Protected quest/key ownership bypasses the normal-slot capacity completely because it does not consume the normal 16-slot grid.
- For normal ownership, a caller-resolved no-new-slot case is not blocked even at 16/16; a normal item requiring a new slot is allowed only below 16 and otherwise rejects as `normal_inventory_full`.
- Malformed caller facts reject deterministically and evaluation does not mutate caller state.
- The slice deliberately does **not** determine stack identity/compatibility or quantities, define item data, mutate inventory/storage, perform pickup/reward/pending-claim transactions, implement quick slots, select quest packaging/save/retry/death policy, add UI, or create runtime wiring.

Executable gate: `tests/test_normal_inventory_capacity_policy.gd` — **PASS**. Independent QA found no defect and confirmed that stack compatibility and `needs_new_normal_slot` remain caller-owned. Prime editor parse and `tests/test_project_resource_paths.gd` also **PASS**.

### Normal realized-stack quantity invariant — §26 / §26.2

- `NormalStackQuantityValidator` is a pure scalar boundary for an already-realized normal stack; it does **not** decide whether an item is stackable or perform stack transactions.
- Existing normal-stack quantities are valid only when the caller supplies a Godot integer in the inclusive range **1..99**.
- Zero, negative values, values above 99, floating-point values and other non-integer inputs reject deterministically. Validation does not mutate caller data.
- The slice deliberately does **not** define stackability or compatibility, merge/split/overflow allocation, item definitions, inventory/storage mutation, pickup/reward/pending-claim transactions, protected quest-item behavior, quick slots, UI, save/retry/death semantics or runtime ownership.

Executable gate: `tests/test_normal_stack_quantity.gd` — **PASS**. Independent read-only QA found no defect. Prime editor parse, the focused item-policy regression set and `tests/test_project_resource_paths.gd` also **PASS**.

### DR-06 direct-hit resolution — Wave 24A

- `DirectHitResolver` is a pure caller-driven direct-hit transaction boundary for the approved DR-06 defense/damage order. It does **not** mutate HP/stamina or own runtime combat state.
- Caller data declares Physical/Arcane domain, contact/projectile delivery, dodgeable/blockable/parryable eligibility, authored guard pressure, triggered critical/weak-point bonuses and the defender's authored evade/defense/facing/resource/mitigation facts.
- Dodge resolves first only for a dodgeable hit inside the authored evade window. Supported block/parry then use the locked facing/eligibility rules; successful block deals zero HP damage and spends authored pressure, insufficient stamina produces guard break plus normal HP resolution, and projectiles cannot be parried.
- Direct damage uses the greater of triggered critical/weak-point multipliers, then the applicable Physical/Arcane mitigation formula with nonnegative rating clamp, explicit positive .5-up rounding and minimum 1 HP for a positive damaging hit. Zero-damage hits remain zero.
- Results are detached/deterministic and validation rejects malformed/non-finite caller facts without mutation. Poise, Burn/Slow, knockback, death/objective events, feedback, RNG/critical chance, runtime wiring and tuning values remain outside this Wave 24A boundary.

Executable gate: `tests/test_direct_hit_resolver.gd` — **PASS**. Independent read-only QA found no defect. Prime editor parse plus `tests/test_m2_foundation.gd`, `tests/test_combat_contact_ledger.gd` and `tests/test_project_resource_paths.gd` also **PASS**.

### DR-06 poise and prototype status state semantics — Wave 24B

- `PoiseResolver` is a pure caller-driven poise boundary. Authored poise damage accumulates below the target threshold; reaching or exceeding the threshold emits one threshold event, resets accumulated poise to zero, and requests the authored interruption/recovery response only when the supplied state is interruptible.
- Guard break remains a separate Wave 24A defense result and is not inferred from poise. Poise threshold and recovery tuning remain caller/authored data; recovery applies only a caller-resolved allowed amount, clamps at zero, and does not create threshold events.
- `PrototypeStatusResolver` admits only the two approved prototype status behaviors: `burn` and `slow`. Active state is keyed by stable status ID, so different IDs may coexist while one ID cannot silently change behavior.
- Reapplying the same status ID never creates an additive stack: magnitude becomes the stronger of current/new magnitude and remaining duration becomes the greater of current remaining/new authored duration.
- Status duration uses deterministic fixed gameplay ticks. Expired states are removed when their remaining duration reaches zero; caller input and returned state stay detached.
- Burn is explicitly damage-over-time and its ticks cannot crit or weak-point. Slow is movement-speed reduction only and cannot alter action clocks or AI reaction timing. This slice deliberately does not invent Burn tick cadence/damage tuning, Slow percentage tuning/application ownership, status immunity/resistance, elemental interactions, knockback, HP mutation, death/objective events, VFX/UI, or runtime scene ownership.

Executable gates: `tests/test_poise_resolver.gd` and `tests/test_prototype_status_resolver.gd` — **PASS**. Prime editor parse plus `tests/test_direct_hit_resolver.gd`, `tests/test_m2_foundation.gd`, `tests/test_combat_contact_ledger.gd` and `tests/test_project_resource_paths.gd` also **PASS**. Repository-wide prime validation `docs/evidence/reference-validation-wave24b-prime.json` discovered **41** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke. Independent worker QA remains pending because the non-Codex worker tunnel was unavailable; no Codex orchestrator was used for this Wave 24B pass.

### DR-04 starter kits and renewable class-resource path — Wave 25

- `StarterKitCatalog` now creates exactly the approved starter equipment identities: Melee one-handed sword + shield, Ranged two-handed bow with empty/forbidden off-hand, and Mage two-handed staff with empty/forbidden off-hand.
- All three starter basic attacks are resource-free `ActionDefinition` instances and retain a usable combat action even when class resources are exhausted. Ranged tracks no ammunition; Mage alone exposes mana; no universal mana/ammo resource was added.
- Melee starter defense capability is explicitly block + parry. Ranged and Mage starter kits expose no baseline block/parry. Projectile basics are authored unparryable under the shared DR-06 rule.
- `StarterCombatTuning` holds reversible first-pass basic damage/guard/poise/timing values plus Mage mana maximum/delay/recovery values as tuning data rather than design canon.
- `ClassCombatRuntime` composes the existing action machine/intent boundary, starter kit, DR-06 direct-hit payload and a deterministic fixed-tick delayed mana regenerator. Mana-spending action commitment restarts the configured recovery delay; recovery then proceeds in combat because no out-of-combat-only restriction exists.
- The live greybox now configures the selected profile class, routes LMB basic attack through the existing input/action ownership path, and shows Mage mana only when applicable. Runtime contact spawning/authoritative hit-shape ownership remains for later genuine encounter integration; this wave does not invent fake enemies to claim it.

Executable gate: `tests/test_starter_class_combat.gd` — **PASS**. Adjacent M2/direct-hit/action-input/main-profile/resource-path gates remain **PASS**. Repository-wide prime validation `docs/evidence/reference-validation-wave25-prime.json` discovered **42** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke.

### DR-05 bounded skill pool/loadout/progression — Wave 26

- `SkillCatalog` defines exactly the approved 18 prototype skills: three active + three passive per class, with the locked names, class ownership, starting 2+2 grants, and explicit mechanical role/dependency IDs (including Riposte after successful parry and Aegis Ward block-style/no-parry semantics).
- `SkillLoadoutState` keeps learned rank ownership separate from equipped 2+2 slots. Every skill has exactly three ranks; starting skills begin at rank 1; alternatives begin locked at rank 0.
- `SkillProgressionService` spends exactly one persistent skill point per rank, records unique progression transaction IDs through the existing claim ledger, rejects duplicates/max-rank/cross-class purchases, and exposes no respec path.
- Learned loadout swaps succeed only at a caller-declared safe interaction while not in Active Combat. Duplicate slot ownership, wrong class/kind, unlearned skills, and temporary-slot remaps reject without changing profile state.
- Temporary combat skills store slot index + displaced learned skill identity, spend no permanent points, do not alter learned ownership, lock only the overridden active slot, and deterministically restore the displaced learned skill when the temporary effect ends.
- `ProfileSnapshot` now persists validated skill state while remaining backward-compatible with earlier empty skill-state saves; new profile creation initializes the locked starting grants/loadout. JSON round-trip accepts integral JSON number representations without weakening rank/slot bounds.
- Exact balance magnitudes/cost/timing/rank scaling remain tuning data. This wave does not fabricate attack hit-shapes, movement displacement, or enemy/runtime encounter ownership for skills whose live effect owner does not yet exist.

Executable gate: `tests/test_skill_loadout_progression.gd` — **PASS**. Persistence/profile regressions remain green. Repository-wide prime validation `docs/evidence/reference-validation-wave26-prime.json` discovered **43** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke.

### DR-01 / DR-02 persistent revisit and coherent death/retry rollback — Wave 27

- `FloorInstanceState` persists one generated identity per prototype floor together with seed/layout revision, defeated actors/boss state, claimed sources, loose-item identity, vendor state, quest state and checkpoints. `TowerFloorStateService` refuses silent seed/instance/layout replacement and exposes only detached ordinary-revisit reads.
- Ordinary revisits preserve the committed floor state and the prototype exposes no player-triggered fresh reset/regeneration path, so cleared floors do not become renewable combat farms.
- `SafeCheckpointState` records a coherent safe boundary identity, map/floor, valid checkpoint/death anchor, player-state bundle, quest-attempt state and monotonic sequence metadata. `SafeCheckpointCommitService` stages and validates the complete profile before durable save and advances the live safe marker only after the atomic save succeeds.
- `ProfileSnapshot` now persists validated tower-floor state and the current safe-state record while remaining compatible with earlier empty fields.
- `DeathRetryCoordinator` restores the latest durable safe generation for failed retry or player death. The restored profile and floor state come from one coherent snapshot, so post-snapshot progression, rewards/claims and enemy outcomes roll back together while floor seed/instance identity remain unchanged.
- Simultaneous player/boss defeat resolves as player death/failure and never pre-commits boss-clear or boss-reward state. Boss-only defeat remains pending an explicit later success transaction. Death/retry adds no currency, durability or generic death fee.
- This wave establishes the persistence/rollback authority; it does not invent a fresh-floor challenge mode, automatic enemy respawn, arbitrary live-combat checkpoints or a reward owner that does not yet exist.

Executable gate: `tests/test_death_retry_rollback.gd` — **PASS**. Persistence/profile/save/skill/resource-path regressions remain green. Repository-wide prime validation `docs/evidence/reference-validation-wave27-prime.json` discovered **44** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke.

### DR-03 exact quest packaging and Tower Sigil/Floor 1 chain — Wave 28

- `QuestCatalog` now exposes exactly **15** prototype definitions: ten primary floor definitions plus five side definitions. Region 3 onboarding/preparation is packaged as stages inside `primary_floor_1`; no sixteenth regional-prelude quest definition exists.
- All definitions stay inside the three approved quest families: Annihilation, Escort and Tower Defense. Floor 1 keeps the approved Annihilation objective allocation. Three regional side slots follow the authored Region 3 anchor/family allocations; the two tower-side family bindings remain explicitly marked playtest placeholders because §53.1 leaves their exact family reversible.
- `Region3PreparationCommitService` atomically commits the required Region 3 preparation stage, permanent Tower Sigil entitlement and initial Floor 1 unlock at one validated out-of-combat safe-save boundary. Before that durable commit, both Sigil travel and physical Floor 1 entry remain unavailable. A failed save leaves the live profile ungranted and reports the transaction as retryable.
- Successful preparation continues the same `primary_floor_1` definition into its `floor_objective` stage, rather than fabricating another quest definition. The grant is protected by a unique claim identity and persists through save/load.
- `Floor1PrimaryObjectiveService` binds a caller-authored designated onboarding group only after the generated floor exists, so Wave 28 does not invent a target count. Incidental kills do not count, duplicate defeat events are idempotent, and all bound required targets transition the quest to `ObjectivesComplete`.
- The Floor 1 objective attempt baseline composes with Wave 27 coherent checkpoints: failed-attempt rollback restores attempt-local kill progress while preserving previously committed Sigil/Floor 1 access.
- Floor 1 turn-in atomically commits `Completed`, persistent Floor 1 primary clear, Floor 2 unlock, completion claim identity and the new safe snapshot. The completion path cannot be granted twice.
- `ProfileSnapshot` now persists validated quest-progress state while remaining compatible with earlier empty quest-progress saves. No final quest prose, narrative stakes, reward numbers, enemy roster or irreversible tower-side placeholder family canon was invented.

Executable gate: `tests/test_quest_sigil_packaging.gd` — **PASS**. Adjacent save/rollback/profile/resource-path gates remain green. Repository-wide prime validation `docs/evidence/reference-validation-wave28-prime.json` discovered **45** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke.

### Wave 29 — genuine encounter ownership + player visual design bible

- `CombatantRuntimeState` is the first authoritative mutable combatant owner above the pure DR-06 resolvers. It owns HP, stamina, Physical/Arcane mitigation inputs and poise accumulation while keeping defender transient facts (evade/selected defense/facing) caller-authored.
- `CombatEncounterRuntime` composes contact identity, direct-hit resolution, HP/stamina mutation, poise application, enemy lifecycle, FULL-AI admission, attack reservation cleanup and shared Active Combat ownership under one stable encounter ID. Invalid resolver input does not consume contact identity; duplicate action/target/hit-interval contact cannot damage twice.
- Enemy defeat now releases FULL-AI admission and outstanding attack reservations and removes the encounter Active Combat reason when the final active hostile resolves. Explicit encounter exit releases encounter-scoped reservations/admissions without fabricating rewards or quest completion.
- Wave 29 also establishes the production player visual contract in `docs/art/PLAYER_VISUAL_DESIGN_BIBLE.md`: one shared 32×32 side-only body, right-facing authoring mirrored left, stable ground/grip anchors, exact controlled palette v1, separate weapon layer, animation production order and combat-cue priority.
- Local Wave 29 art references now exist under `res://assets/art/player/` and `res://assets/art/palettes/`: a 32×32 player side-body reference, an enlarged review sheet with anchors, and a 24-color palette strip. These are provenance-recorded project-local assets and do not claim that the full animation set is already complete.

Executable gate: `tests/test_combat_encounter_runtime.gd` — **PASS**. Updated asset provenance validation is **PASS** with the three real Wave 29 assets recorded. Repository-wide prime validation `docs/evidence/reference-validation-wave29-prime.json` discovered **46** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke.

### Wave 31A — first production enemy behavior slice

- `EnemyArchetypeDefinition` now encodes the required threat-card contract: preferred range, observable triggers, signature commitment, recovery opportunity, defensive limits, counterplay, group role, objective behavior, allowed variants and authored tactics.
- `EnemyArchetypeCatalog` defines the first four production mechanical roles — Duelist, Bruiser, Defender and Marksman — without turning those mechanical names into lore canon.
- `EnemyTacticalSelector` selects only from caller-supplied observed facts. It respects reaction-delay admission, uses decayed last-known information after sight loss, adjusts toward authored preferred range, prioritizes contested objectives where supported, and only selects a signature commitment when reservation/cooldown eligibility exists. It never reads hidden player HP/resources/cooldowns or unseen live position.
- The four archetypes integrate with the Wave 29 encounter owner and therefore consume counted FULL-AI admission and attack reservations rather than acting as uncounted background attackers.
- This is the mechanical first-enemy slice only. Production enemy silhouettes, sprite animation and attack-telegraph art remain Wave 31B so visual completion is not overstated.

Executable gates: `tests/test_enemy_first_slice.gd`, `tests/test_enemy_tactical_selector.gd`, and `tests/test_enemy_slice_encounter_integration.gd` — **PASS**. Existing AI observation/lifecycle/reservation/full-AI and combat-encounter regressions remain green. Repository-wide prime validation `docs/evidence/reference-validation-wave31a-prime.json` discovered **49** `test_*.gd` gates and reported **0 failures** plus a passing main-scene headless smoke.

### Development-only §61.1 observability composition

- `DevelopmentObservabilitySnapshot` composes existing read-only action-intent/action-machine state, current contact identities for that action instance, the first ordered blocker reason plus its owning source for a named operation, one save-request status with its slot/request identity, and a bounded AI observation-memory summary.
- Contact inspection exposes only action-instance ID, stable target ID and authored hit-interval/index. It is deterministically ordered and returns detached dictionaries; it does not calculate or expose damage/defense results.
- AI debug enumeration is bounded and deterministically newest-first, returning only observed cue/source/state/position, observed age/tick, caller-decayed confidence and expiry. It exposes no raw input, live target handle, current hidden state, tactics or threat score.
- All composed branches are detached from their source systems. Mutating a development snapshot cannot mutate action state, contact history, operation blockers, save status or AI observation memory, and creating a snapshot does not advance/purge/commit source state.
- The snapshot is explicitly marked development-only. It is not wired into the existing greybox `DebugHUD` yet because the runtime scene does not currently compose live contact-ledger, operation-guard, save-coordinator and AI-memory instances to display truthfully.

Executable gate: `tests/test_development_observability.gd` — **PASS**.

### Native audio foundation — §37.1

- `AudioService` is a global native-Godot audio boundary; gameplay code does not need direct asset paths.
- Required prototype buses are created idempotently at runtime: Master, Music, Gameplay SFX, UI and Ambience.
- `AudioEventDefinition` provides typed named-event metadata for bus, Critical/Gameplay/Decorative priority, positional policy, overlap limit, one-shot/loop lifecycle, duration hint, variation allowance, stop-on-owner-exit/explicit stop condition, and an optional future `AudioStream` asset.
- Music/UI event metadata rejects positional playback, and unmanaged looping definitions reject missing stop paths.
- Bus volume and mute APIs are exposed for the later settings/accessibility UI without creating final audio content.
- The locked §37.1/§41 target of at most 32 simultaneous important positional SFX now has aggregate token admission: slots 1–32 are admitted, a 33rd is rejected, and released capacity can be reacquired. No priority-stealing policy is inferred yet.
- Fixture-only event lifecycle admission now enforces each registered event's authored overlap limit and correlates Critical/Gameplay positional instances with the shared important-positional budget; Decorative positional events remain outside that *important* budget.
- Owner-bound loops require an owner identity and can release all owned instances on owner teardown; explicit-stop loops can be terminated by lifecycle token without requiring an owner. Ending an event releases per-event and aggregate bookkeeping exactly once.
- Lifecycle-owned aggregate leases are protected from the older public/manual aggregate-slot release API, so a colliding token cannot under-report the ≤32 budget while the lifecycle event remains active. Clearing the event-definition registry does not orphan already-admitted instances; they retain bookkeeping and can still terminate cleanly.
- Player-facing Master/Music/Gameplay SFX/UI/Ambience volume (0–100%) and mute preferences persist in `user://settings.cfg`, separate from campaign slots, and apply immediately through the existing `AudioService`/native `AudioServer` buses. Persisted malformed/non-finite volume data recovers deterministically to 100%, finite out-of-range values clamp, malformed mute data recovers unmuted, and the public native volume API rejects non-finite input without mutating a bus.
- Audio settings share the existing pre-game/pause Accessibility & Controls panel. All five bus sliders/mutes plus an audio-only reset remain keyboard reachable through the focus-following 640×360 scroll surface at the maximum current UI/text scale. Audio reset restores 100% + unmuted without resetting Run/input/UI/text/camera preferences or campaign data.
- Audio values are staged during settings load and applied only after existing persisted-input validation succeeds, so a conflicting binding file cannot partially apply audio state.
- No final music, SFX, VO, adaptive-music state machine, priority-stealing policy or asset-specific event catalog has been created.

Executable gates: `tests/test_audio_foundation.gd`, `tests/test_audio_lifecycle.gd` and `tests/test_audio_settings.gd` — **PASS**.

### Asset provenance / import-policy metadata tooling

- `AssetProvenanceManifest` validates recorded source/creator/license/attribution/modification/date/responsible-agent metadata and the locked pixel-art metadata fields without claiming legal sufficiency.
- The validator rejects unsupported/unknown license categories, malformed field/schema types, duplicate project paths and impossible calendar dates instead of coercing malformed data into acceptance.
- AI-origin assets must still carry ordinary source/creator/provider evidence; the validator does not certify provider terms. The checked-in manifest remains empty rather than fabricating provenance for existing files.

Executable gate: `tests/test_asset_provenance_manifest.gd` — **PASS** after independent compliance/QA review.

### Production `res://` reference audit tooling

- `ProjectReferenceAudit` scans only production `project.godot` plus recursive `.gd` / `.tscn` / `.tres` files under `res://src`; docs, tests, tools and generated evidence are not treated as production reference owners.
- Literal `res://` references are collected deterministically and de-duplicated, including legal paths containing spaces or Unicode.
- Path safety rejects real `.` / `..` traversal segments and invalid filename segments without falsely rejecting legal filenames that merely contain `..`.
- Missing/unsafe references report their owning production source and have no gameplay/runtime side effects.

Executable gate: `tests/test_project_resource_paths.gd` — **PASS** after independent QA review.

### Locked cross-system utility — Danger

- Exact §16 level-deficit mapping implemented as a pure `DangerEvaluator`.
- DANGER I–V labels are stable.
- DANGER III–V explicit menu-travel confirmation policy is exposed without creating a level gate.
- Equipment/content does not alter the ordinal calculation.

Executable gate: `tests/test_danger.gd` — **PASS**.

### Input ownership and shared operation guards

- Modal UI ownership blocks gameplay/combat routing while a modal is open.
- Gameplay and pause now share one `InputOwnership` instance instead of maintaining separate modal state.
- Movement polling, Interact/climb, pointer-driven body/weapon aim and camera wheel zoom are suppressed while any non-pausing modal owns input; dash/dodge/run admission routes through the same ownership boundary.
- The input that closes a modal is suppressed until release so it cannot pass through as an attack/use action; `InputOwnership` observes releases in `_input`, before GUI consumption, so suppression cannot remain stuck when a modal consumes the release.
- Nested modal ownership restores the prior owner cleanly.
- Shared operation guard supports composable blockers with stable reason IDs/text for manual save, Tower Sigil travel and services.
- Active-combat and unsupported-transient blockers are represented without choosing quest/death/replay policy.
- Independent blockers release by token or source and cannot silently clear unrelated restrictions.

Executable gates: `tests/test_input_ownership.gd` and `tests/test_operation_guard.gd` — **PASS**.

### Save-request / safe-boundary coordinator — §32.3

- `SaveRequestCoordinator` distinguishes autosave, checkpoint and manual requests without defining when gameplay content must trigger them.
- Unsafe autosave/checkpoint requests remain pending and retain only request metadata plus a snapshot-provider callback; no `ProfileSnapshot` is captured or serialized at request time.
- Shared guard reason ID/text is surfaced while a queued request is blocked. Unsafe manual requests return the restriction immediately instead of advertising pending or durable progress.
- Any failed new request on a slot (manual restriction, invalid kind, or unavailable provider) returns its own failure without overwriting the observable pending status of already queued work for that slot.
- Repeated unsafe autosave/checkpoint requests for one slot stay bounded to one pending entry and coalesce to the latest request/provider; the superseded provider is never invoked.
- Releasing the final guard blocker does not synchronously capture state. The boundary owner finalizes its coherent gameplay state first, then calls `process_pending()`; only then does the coordinator revalidate each queued provider, invoke it, and commit the fresh snapshot through `SaveService`.
- Per-slot status reports pending/succeeded/failed state, request kind/ID and save error data; one slot's queued/failed work does not write or corrupt another slot.
- This coordinator predates and does not itself implement the now-resolved DR-01 replay or DR-02 death/checkpoint restoration policy; the future rollback owner must compose this coordinator with the approved §58 semantics.

Executable gate: `tests/test_save_request_coordinator.gd` — **PASS**.

The runtime InputMap now seeds the complete locked prototype default action-name surface from §13.2: movement/aim-facing controls, basic attack/block/parry, run/dodge/dash, Interact, two active slots, four consumable slots, Inventory/Skills/Quest Log, Map, Tower Sigil menu and Pause. Defaults remain rebindable.

### Pause / focus-loss boundary

- Gameplay pause is a real `SceneTree` pause, so player/movement processing stops instead of merely hiding input.
- A dedicated `PauseCoordinator` remains active while paused; the gameplay root and player remain pausable.
- Escape opens/closes the pause boundary using modal input ownership.
- Resume uses the button-down boundary, suppresses the left-click attack while unpausing, and the shared input owner clears that suppression on the matching mouse release; the click cannot pass through or remain suppressed for an extra click.
- Application focus loss enters the same pause boundary and exposes `focus_loss` as an observable reason.
- This does not clear active-combat state or authorize save/travel; those remain separate shared-guard concerns.

Executable gate: `tests/test_pause_coordinator.gd` — **PASS**.

### Player-usable accessibility/settings slice — partial QA-ACCESS

- Main profile selection establishes keyboard focus on the first save slot; opening pause establishes focus on Resume.
- One shared greybox `Accessibility & Controls` panel is reachable before gameplay from the main profile screen and from pause; the pause instance uses nested modal input ownership.
- Opening settings establishes keyboard focus on the first setting. Escape/Back restores the correct prior surface and returns focus to the settings trigger; nested pause settings do not accidentally resume gameplay.
- Run supports the locked hold/toggle alternative. Switching mode resets stale toggle state; the default remains Hold.
- Reduced motion applies immediately to the camera and suppresses look-ahead plus screen shake without changing gameplay/AI rules.
- Screen-shake intensity is adjustable from 0–100%; 0 disables shake and other values scale bounded shake requests.
- UI scale and text size are separately selectable at 100%, 112.5% and 125% as a **reversible implementation/test range**, not a design lock. Both persist in `user://settings.cfg` alongside the existing presentation preferences.
- Pre-game profile/pause controls and the shared settings panel use a UI-only Theme scaling layer; changing UI/text scale does not change the world `PixelCamera` zoom step or zoom vector.
- The shared settings panel and pre-game profile shell use focus-following scroll containers where enlarged content can exceed the fixed panel viewport. At the largest supported prototype UI/text settings, Settings Back, profile slots/settings trigger, empty-slot name/class/Create flow, pause Resume/Settings and pause Settings Back remain keyboard-reachable inside the 640×360 canvas.
- Non-finite UI/text scale values are ignored by runtime setters and recover to default scale when loaded from settings storage instead of poisoning the UI Theme.
- The player-facing control surface lists the locked prototype action-name set, captures keyboard or mouse bindings through `InputBindingService`, clearly rejects conflicts without changing the prior binding, and provides `Reset Control Defaults` recovery.
- Run mode, reduced motion, shake intensity and input bindings persist in `user://settings.cfg`, separate from campaign save slots. Persisted presentation settings apply when gameplay is instantiated and reset bindings survive reload.
- Persisted bindings are staged and conflict-validated as one set before commit; malformed/conflicting settings return `ERR_INVALID_DATA` without partially mutating the prior InputMap or unrelated accessibility values, preserving the same conflict boundary across reloads.
- Current settings/binding state and conflict/rebind results are visible as text feedback.
- Per locked §43, the prototype remains English-only, but dynamic profile/accessibility/rebind/status strings now pass through Godot `tr()` as complete messages where practical. Class and control labels have explicit presentation mappings while persisted/stable IDs remain unchanged; unknown IDs degrade to readable fallback text rather than becoming hidden translation keys.
- No translated catalog, locale selector, CJK/RTL/voice-localization promise, pseudo-localization or locale-aware list/key-name formatting has been added. This is localization-readiness only, not localization completion.
- This is **not** QA-ACCESS completion: flash reduction, color-independent critical-cue treatment, subtitle/text coverage, sound-off/mono evidence, gameplay-assist factors, manual high-contrast/magnification checks and real assistive-technology/screen-reader validation remain unimplemented or unverified.

Executable gate: `tests/test_accessibility_settings.gd` — **PASS**.

### New-game/profile shell

- Main scene is now a thin profile front-end; the former movement greybox is isolated as `src/app/gameplay.tscn`.
- Exactly three save slots are surfaced.
- Empty slots create a named protagonist and select one permanent class identity: Melee, Ranged or Mage.
- Existing slots load the saved profile and enter the gameplay greybox.
- The main scene exposes its save root as data, allowing isolated automated/profile environments without changing the production `user://saves` default.
- Class selection currently stores identity only; no DR-04/05 starter kit, resource model or skill roster is inferred.
- New profiles start at Level 1, 0 XP and 0 unspent skill points.

Executable gates: `tests/test_profile_creation.gd` and `tests/test_main_profile_flow.gd` — **PASS**. The end-to-end gate verifies empty-slot selection → profile creation/save → gameplay entry → fresh main-scene reload → existing-slot gameplay entry using an isolated test save root.

### Persistent one-shot claim ledger

- `ClaimLedger` records stable claim IDs and their optional stable source IDs.
- First claim succeeds; duplicate claim fails.
- Malformed claim/source IDs are rejected.
- Claim state serializes into the profile snapshot and round-trips through the atomic save service.
- This establishes duplicate-prevention identity only; the now-resolved DR-01/02 contract defines which coherent snapshot is restored on replay/death, but that restoration behavior is not implemented by `ClaimLedger` itself.

Executable gate: `tests/test_claim_ledger.gd` plus M0 round-trip coverage — **PASS**.

## Production art/image pipeline — Waves 30–36 partial implementation

- The repository contains a deterministic local pixel-art production pipeline at `tools/art/generate_pixel_asset_batches.py` plus later phase scripts, but procedural V01 inventory is no longer treated as equivalent to the promised image-generator source art. Exact-source V02 conversion is tracked separately.
- Player art has all **17 required body animation categories** as 32×32-cell V03 sheets derived from the exact user-provided `nice_journey_main_character_sprite_batches_transparent.zip`. Animation identity was assigned by direct visual inspection of the rendered poses/motion rather than by trusting the ZIP member filenames; exact sources, hashes and row→semantic classifications are preserved under `assets/art/generated_sources/imagegen/player/main_character_batches_v03/`. The AnimationLibrary references V03 for every category; the superseded V02 player-body sheets were removed only after focused and full repository validation passed. Runtime reachability is tracked separately from asset presence: locomotion/climb and starter Attack/Cast are live; Melee RMB Block and F Parry now drive authoritative `ClassCombatRuntime` defense state and the corresponding V03 body animations; authoritative player Block/Parry/Hit/Death combat outcomes bind through `PlayerRuntimePresentationBinder`; Interact/Pickup/Use Item/Sleep have a committed-presentation hook but remain pending their genuine gameplay/service owners; Heavy Attack remains intentionally unbound until a real authored heavy action exists, because the master forbids inferring an extra attack binding from the animation alone.
- Player sprite presentation was migrated from manual runtime frame stepping to **`Sprite2D` + `AnimationPlayer`**. `res://src/player/presentation/player_body_animation_library.tres` owns texture selection, hframes, frame keys, timing and loop behavior as inspector-editable animation data. `PlayerBodyAnimator` now selects semantic animation names only; it contains no art preloads and no manual frame counter/FPS loop.
- The AnimationLibrary contains all 17 semantic player animations. Asset presence is not treated as proof of gameplay reachability: future source replacements update editor/resource texture keys, while each semantic animation still requires a genuine gameplay/runtime owner before it can be called live.
- Starter Sword/Bow/Staff/Shield presentation was migrated from hardcoded textures/offsets in `starter_weapon_visual.gd` to inspector-editable `StarterWeaponVisualProfile` `.tres` resources for Melee/Ranged/Mage. Texture, position, rotation, scale and shield visibility now live in resources; runtime code only applies the selected class profile.
- Player presentation thresholds that are authored/tunable (`stationary_speed_epsilon`, run threshold ratio, weapon-behind vertical aim threshold and base weapon z-index) are exported/scene-authored rather than buried as runtime magic numbers.
- Starter sword/shield/bow/staff, arrow/Arcane projectiles, six player combat VFX and the full earlier-generated telegraph/cue batch are now exact-source-derived V02 assets. Replaced player/VFX/telegraph V01 gameplay PNGs were removed; exact source sheets remain retained under `assets/art/generated_sources/imagegen/` with derivation/provenance records. The V02 VFX/telegraph set now also has a shared inspector-first `EffectVisualProfile`/`EffectVisualCatalog`/`EffectSpritePresenter` presentation layer using `Sprite2D + AnimationPlayer`; all 19 profiles own their texture and editable animation resource outside combat-resolution code, and runtime presentation contains no hardcoded art paths or manual frame stepping. The live player now binds its three starter basic-action commit points through inspector-authored `ActionEffectBinding` resources: Melee → generated light slash trail, Ranged → generated release flash, Mage → generated Arcane cast burst. Gameplay legality/timing remains owned by `ActionStateMachine`; presentation only listens to the authoritative commit signal.
- All **12 reusable enemy archetypes** now use generated-source-derived V02 48×48 idle/move/windup/release/hit/death gameplay derivatives through inspector-authored `EnemyVisualProfile` + external `AnimationLibrary` + `Sprite2D` + `AnimationPlayer` presentation scenes. Duelist/Bruiser/Defender/Marksman retain their earlier exact-source derivations; the Duelist was additionally re-cropped after renderer evidence caught reference-sheet header pixels. The remaining eight — Skirmisher, Assassin, Mobile Ranged, Caster, Support, Summoner, Flying Harrier and Controller/Disruptor — were accepted from `D:/nicejourney/Nice_Journey_Remaining_Enemy_Generated_Assets.zip`, preserved byte-for-byte under `assets/art/generated_sources/imagegen/enemy_refs/remaining8/`, deterministically cropped/background-removed/downscaled into 48×48 V02 pose cells by `tools/art/derive_remaining_enemy_v02.py`, wired into their existing inspector-owned AnimationLibraries/scenes, and visually reviewed in-engine at `docs/evidence/renderer/remaining-eight-enemy-showcase-v02.png`. Crop QA caught and corrected stray neighboring-source fragments on Assassin and separated projectile/orb components on Mobile Ranged and Controller/Disruptor before final validation. All replaced V01 gameplay PNGs for these eight were removed only after V02 references, focused tests, provenance, resource-path checks and broad validation were green.
- Region 3 visual inventory covers all **8 functional + 12 decorative** town structures, a prop sheet, road/ground tiles and ruins modules. The eight locked functional roles now use accepted ChatGPT-generated V02 building art through inspector-owned `Region3FunctionalBuildingVisualProfile` resources, a validated role catalog and a generic nearest-filtered `Sprite2D` presenter with no hardcoded art paths. The exact 1254×1254 generated inputs from `D:/nicejourney/Nice_Journey_Region3_Functional_Buildings_Generated_Assets.zip` are preserved byte-for-byte under `assets/art/generated_sources/imagegen/region3/functional_buildings_v02/`; seven sources touched a source edge, so canonical source copies add only deterministic 32 px transparent padding before nearest-neighbor reduction. `tools/art/region3_building_v02_deriver.py` records the source/normalized/derivative hashes and produces a 224×224 Central Tower plus seven 192×192 service-building derivatives without repainting. The replaced functional-building V01 PNGs and their obsolete provenance entries were removed only after V02 profile, inventory, provenance, route and broad validation were green. The master §24.12 town footprint remains represented by `src/world/region3/layout/region3_authored_town_layout.tscn`: all 20 stable structure anchors occupy the exact authored reserved-lot centers for the 160×160 logical map hypothesis, with the 8/12 manifest contract mechanically revalidated and placement data kept scene/Inspector-authored. Because the accepted generated service façades all expose a readable south/front entrance and the master locks only the Central Tower door direction, the five earlier reversible east/west/north service-door hypotheses were re-authored south and their one-tile approach/connectors moved accordingly rather than leaving visual doors inconsistent with interaction anchors. The circulation draft remains one connected graph: the constrained inner loop is three tiles wide where reserved lots do not physically permit the four-tile target, four outward approaches remain four tiles wide, service connectors remain three tiles, all eight functional approach tiles touch the graph, disconnected connector mutations are rejected, and validation is non-mutating. Renderer evidence: `docs/evidence/renderer/region3-authored-town-layout-v02-functional-buildings.png`. Decorative structures 01–10 are accepted V02. Decorative 11–12 plus town props, ground/road, ruins, south-outskirts support and east-risk-zone support currently have V02 preview derivatives wired through inspector-owned profiles, but Batch 2 is not accepted. `D:/nicejourney/Nice_Journey_Region3_Decorative_V02_Batch2.zip` is truncated/corrupt (SHA-256 `bedd828af892f70e8604c36eee9ad9452757e988c88f729dcc238194f673ad1e`), and the seven exact generated source PNGs are not present elsewhere in the workspace; preview derivatives are not provenance substitutes. Physical structure collision and collision-aware traversal are now executable and green. Region 3 also owns a scene-level `RouteNavigator` backed by `Region3RouteNavigationGraph`: it compiles the 13 inspector-authored route lines into one deterministic connected AStar route graph, adds functional approach points where they touch route segments, rejects disconnected geometry, refuses unauthored off-network starts instead of silently snapping, and exposes paths from all four main approaches/eight functional approaches to the Central Tower approach. It also supports caller-bounded explicit rejoin planning for escort-style separation: the result reports the nearest route position, physical rejoin distance and onward path, rejects over-distance or unauthored-goal requests, uses only temporary graph state during the query, and never moves the actor. `Region3RouteFollowDriver` now consumes that route/rejoin data to physically move a caller-owned `CharacterBody2D` with caller-authored speed/tolerance policy, explicit wait handling and ordinary collision; it reports collision for caller-owned repath/failure decisions, cannot teleport through blockers, and clears ownership on rejected configuration. Authored Region 3 escort actor/goal/failure-policy content remains separate work.
- Tower art includes a common modular tileset plus visual categories for combat/safe/reward/vendor/secret/elite/objective/boss rooms. Those eight room categories have inspector-owned `TowerRoomVisualProfile` resources, a validated catalog and a generic nearest-filtered `Sprite2D` presenter with no hardcoded art paths. A 640×360-safe comparison scene at `src/world/tower/presentation/tower_room_visual_showcase.tscn` now renders all eight semantic profiles together; `tests/test_tower_room_visual_showcase.gd` verifies unique category/profile coverage and nearest filtering, and `docs/evidence/renderer/tower-room-visual-showcase-v01.png` records the current renderer result. Current textures remain provisional V01; live procedural floor composition/placement and generated/final art remain separate work.
- UI art includes the exact **18 skill icons**, three class icons, three quest-family icons, Tower Sigil/map markers, and Burn/Slow status icons. All 28 current UI image assets now have inspector-editable `UiIconProfile` resources in one `UiIconCatalog`; the profile-creation screen visibly consumes the catalog for Melee/Ranged/Mage class selection through a nearest-filtered `TextureRect`, with no hardcoded art paths in the runtime presenter. `MapViewService` and the GameplayRoot-owned `MapMenu` implement the locked World/Region/Tower-Floor layer shell over stable IDs: World exposes exactly 12 region identities with only Region 3 playable, Region exposes the authored Region 3 revision/160×160/20-structure contract, and Tower exposes current floor identity/revision/clear/checkpoint metadata plus only physically discovered room geometry. `FloorInstanceState.discovered_room_ids` is backward-compatible persistent state; `TowerFloorDiscoveryService` validates discovered IDs against the committed manifest, the runtime composer gives every traversable room an independent discovery trigger derived from authored walkable geometry, GameplayRoot records the arrival room and later physical room entries, and map summaries omit all undiscovered room IDs plus room tags/module identities. Region quest/risk discovery is not fabricated. For Tower rooms, discovered `combat`/`elite`/`boss` tags are reduced to stable `risk:*` markers only after discovery, without exposing unrelated tags or module metadata. The Tower Floor layer renders filtered discovered-room rectangles through `MapRoomCanvas`; anchored checkpoint positions remain hidden until their room is discovered, room-to-room navigation edges are exposed only when both authored endpoints are discovered, and the canvas independently rejects markers/connections outside its visible discovered-room set while rendering risk with distinct shapes rather than color alone. It receives no full manifest and cannot expose undiscovered room identities. Tower travel eligibility is also consumed read-only from `TowerAccessMenuService` with the shared `GameplayOperationGuard`, so selectable floors and authoritative Sigil/operation-blocked reasons can be inspected while `travel_action_available` remains false and the map cannot invoke travel. That snapshot also preserves player level, destination Recommended Level/Danger, actually-active primary objective ID, elite target and boss warning; special hazards remain explicitly unknown because the current floor catalog has no authored hazard field. The Region layer now uses `Region3MapSnapshotService` + `MapRegionCanvas` to derive graphical data directly from the validated authored town scene: exact 160×160/town/plaza bounds, all 13 inspector-owned road/connector polylines and only the fixed public Central Tower landmark. Service discovery is now live: admitted Region 3 service interactions mark only the exact authored service identity, and the Region map reveals used services while unused services remain hidden; discovery is banked only by a later legitimate coherent save. Explored-subzone discovery is now profile-backed, physical-traversal-driven and rendered from the existing authored zone rectangles; opening the map is read-only and cannot discover a zone. Exact quest-marker tiles, Region checkpoint geometry and concrete Region danger values remain withheld until genuine authority exists. Gameplay now also replaces the former debug-only resource text with `CombatHUD`: HP/stamina are live universal rows, Mage alone exposes mana, the approved Ranged starter creates no ammunition row, defense mirrors `ClassCombatRuntime`, cooldowns are read from `ActionStateMachine`, and active Tower context derives floor identity/Recommended Level/Danger from the live floor host + `PrototypeTowerFloorCatalog` + shared `DangerEvaluator`. `QuestTrackerViewService` supplies only the current Tower floor's active primary objective: Annihilation defeated/required counts, Escort route/wait state, Tower Defense wave/objective-HP state, and Floor 10's dedicated boss-completion state. Objectives-complete state is represented explicitly, unrelated side quests cannot become the Tower tracker, malformed objective state fails closed, and `timer_state_available` stays false because no live quest-timer owner exists. Numeric values accompany bars, and the HUD consumes the same scalable UI/text presentation layer while remaining inside the 640×360 minimum canvas at 125% scale. `SkillHudViewService` also exposes exactly two persisted active-skill loadout slots using the authored `SkillCatalog` display names/ranks plus existing `UiIconCatalog` skill icons. The surface is explicitly loadout-only because active-skill execution is not yet wired: it reports `runtime_action_available = false`, invents no per-skill cooldown/action-ready state, and preserves temporary quest-skill IDs without fabricating unknown catalog metadata or icons. Burn/Slow HUD presentation is now driven from the authoritative bound player combat runtime and shows exact remaining ticks. Supported status states also hand off into `ClassCombatRuntime` at normal encounter teardown and survive coherent save/load; production Burn cadence/damage and Slow aggregation/application values remain unauthored and are not invented. A distinct Tower minimap reuses the filtered discovered-room snapshot and shows the real player tile only while it lies inside already-discovered room geometry, so neither hidden rooms nor the player marker leak through undiscovered space. `QuestLogViewService` + `QuestLogMenu` expose persisted quest entries through the authored `J` action, including exact quest/family/state/stage identity, scope/floor location, family-specific objective progress, recorded historical leave/failure reason IDs and completed outcome state. They also expose persistent §26.2 pending normal rewards with exact claim/source/item IDs and quantities after source closure. `PendingRewardClaimTransactionService` + GameplayRoot now provide the required outside-Active-Combat Claim action: capacity and duplicate identity are revalidated, shared service blockers disable Claim in Active Combat, and successful claims mutate only live profile item/economy/claim state. An accepted pending-reward claim now requests the shared safe autosave path. A succeeded autosave marks the claim durable; a blocked autosave remains visibly pending and is not advertised as durable, preserving the coherent-snapshot rule. Acceptance conditions and generic leave/retry consequences remain explicitly unavailable until production policy owners exist; malformed quest/economy state fails closed. Quest Log input shares modal ownership, Escape-back suppression and the global 100/112.5/125% UI/text scale. `SkillTreeViewService` + `SkillsMenu` now expose the authored `K` read-only six-skill class tree and persistent build state: ranks/learned state, exact two-active/two-passive equipped IDs, approved display/mechanic/resource/prerequisite identities, max rank 3, one-point rank cost and exact temporary override state, using the existing skill icon catalog. Training Hall service admission now owns durable progression mutation: rank spending requires explicit confirmation, accepted rank purchases and active/passive loadout swaps save atomically through stable transaction identities, and live Active Combat is rechecked before each mutation. Closing the Training Hall Skills session clears mutation hooks; ordinary `K` reopens the same Skills surface read-only. Prerequisite-event satisfaction is still not inferred. Skills input shares modal ownership, Escape-back suppression and the same global accessibility scale. New profiles now persist only the locked DR-04 starter equipment identities through `StarterEquipmentCatalog` + `StarterInventoryInitializationService`: Melee sword+shield, Ranged bow, Mage staff; all normal inventory slots, Gold, protected/quick items, armor/accessories and unauthored rarity/affix/upgrade metadata remain empty, while legacy/existing ownership is never rewritten. The persistence validators now tolerate only finite integral JSON numeric representations for stored Gold/quantities/ranks while live normal-stack API validation remains integer-only. The same persisted/live split now covers `EconomyState` pending-reward quantities, `VendorStockState` stock/prices and non-empty `StorageState`: JSON-normalized integral floats validate safely and storage load canonicalizes capacity/quantities/upgrade ranks back to integers, while live vendor/reward/storage authoring paths retain their strict integer contracts. New profiles now explicitly initialize an empty 64-slot `StorageState`; legacy profiles with empty storage remain readable. `ProfileStorageTransactionService` stages Inventory/Storage/ClaimLedger together, uses the existing duplicate-resistant transfer service, validates the whole candidate `ProfileSnapshot` before committing any live owner, lazily applies the default storage state only after a successful legacy transfer, and marks accepted deposit/withdraw changes live-but-unbanked until the next DR-02 safe snapshot. The authored `r3:functional:06` Storage House now owns `Region3StorageServiceInteraction` at its existing authored approach tile; the trigger uses `InteractionCommitAdmission`, shared modal ownership and `GameplayOperationGuard.OP_SERVICE`, rejects remote/modal/Active-Combat use, and emits only the exact Storage House service request. The real Main create/load path now calls `GameplayRoot.ensure_starting_world()` and instantiates the authored Region 3 town through `Region3TownSessionHost`. New profiles start at the authored Quest Hall approach, capture actual live resource/combat state, and durably commit `safe:region3_start` using existing `region:3` + `checkpoint:region3_town` identities; existing Region 3 safe snapshots restore their saved location. Tower entry suspends this same Region 3 runtime and Tower exit resumes it at the recorded pre-entry position, while the old foundation greybox remains only an isolated development/test shell. The Storage House trigger is a genuine live hub-service owner and GameplayRoot now binds its deposit/withdraw menu to `ProfileStorageTransactionService`; accepted transfers mutate exact persisted inventory/storage state and remain subject to the coherent-save banking rule. `InventoryViewService` + `InventoryMenu` expose the authored `I` read-only inventory/equipment surface with exactly 16 normal slots, five equipment slots, Gold, protected quest/key items, four consumable quick references and only actually persisted item metadata. Search/sort are presentation-only and missing metadata is explicitly unavailable. `InventoryDestroyTransactionService` now enforces explicit confirmation for normal-item destruction, rejects protected/Tower-Sigil/equipped ownership, preserves partial-stack identity and quick references, clears stale references only when the full instance is removed, and records duplicate-resistant transaction identity. `InventoryDropTransactionService` moves only a complete owned stack into a dedicated persistent `player_drop` floor record at a caller-supplied finite world position, preserves exact realized metadata/source identity through JSON save/load, clears stale quick references, rejects protected ownership and partial-stack splitting, and restores the exact instance on pickup without rerolling/merging; full inventory leaves the source in place, and ordinary random-loot entries cannot be consumed through the player-drop path. `PlayerDropStateValidator` is now shared by that transaction path and `FloorInstanceState` persistence validation, so malformed `player_drop:*` records (wrong source identity, invalid item state, nonfinite position or non-dictionary corruption) fail whole-profile validation before save/load while existing resolved random-loot entries remain compatible. The `I` UI now exposes confirmed Destroy for selected normal stacks plus full-stack Drop in both active Tower floors and Region 3. Tower drops remain owned by `FloorInstanceState.loose_items`; Region 3 drops are independently owned by `ProfileSnapshot.region_state.loose_items`, bound to the authored Region 3 map revision and stored in Region-local coordinates. The Region transaction rejects nonfinite or out-of-map positions before mutating ownership; restored finite positions are also validated against the active authored layout during Region initialization, read-only map inspection, pickup reconstruction and both transaction directions. An off-map persisted drop fails closed without deleting the saved source or activating the Region runtime. Structural profile validation still checks portable record shape/finite coordinates without guessing an unbound layout's tile size. Dedicated transaction/runtime regressions cover invalid saved locations, protected items, revision mismatch, duplicate attempts, no-mutation rejection, exact item metadata/quick-reference cleanup, real atomic JSON save/load and one-time collection. `PlayerDropPickup` now provides the shared collision/self-repickup behavior, with Tower/Region-specific pickup scenes preserving separate runtime owners. Fresh/restored Region drops reconstruct on Region activation, block immediate self-repickup until the player exits, preserve the persisted source on full-inventory rejection, restore the exact instance/metadata on retry, and remain live-but-unbanked until the next coherent safe snapshot. Whole-profile validation rejects malformed or unauthorized Region loose-item record families, and Region Drop never creates or mutates Tower floor persistence. `NormalItemOwnershipValidator` now also rejects duplicate normal item-instance ownership across portable inventory, Storage House, Region player drops and player drops on separate Tower floors; equipment and quick-slot references do not count as duplicate ownership, and ordinary resolved loot remains compatible. This profile-wide gate protects the standard SaveService and staged item mutations without adding new gameplay data. Equip/unequip and four quick-slot bind/clear actions are wired through GameplayRoot. New quick-slot binding fails closed unless `ItemCategoryCatalog` explicitly proves the item definition is `consumable`; unauthored, material, or gear categories cannot be silently bound, while legacy persisted references remain load-compatible and are identified as unvalidated when category authority is absent. Playtest tonic use via the 1–4 shortcuts now has an Inspector-authored consumable effect/transaction owner, while other consumables still fail closed without use authoring; merchant buy/sell can operate only on persisted vendor stock/prices, which are currently absent in production. Inventory uses shared modal ownership, `I`/Escape suppression and the same 100/112.5/125% accessibility scale with an internal scroller that keeps the shell inside 640×360 at maximum scale. `LevelThresholdTuning` supplies the §19.5 Level 1–10 progression-threshold table as inspector-editable data: the default initial hypothesis is exactly 100/200/…/900 XP for Levels 1→2 through 9→10 (4,500 cumulative XP to cap), alternate positive tables remain reversible tuning, and Level 10 exposes no Level 11 threshold. `ProfileSnapshot.automatic_stats` is now the backward-compatible durable owner for automatic level-growth stats; pre-field schema-1 saves remain readable with an explicitly empty automatic-stat state rather than invented values. `LevelProgressionService` stages level, XP, unspent skill points, automatic stats and the stable claim identity in one validated profile candidate, rejects live-runtime stat mirrors that disagree with the persisted source of truth, and aggregates multi-level transitions exactly once. `LevelProgressionProfileCommitService` supplies the concrete SaveService transaction boundary: it applies the staged runtime stat mirror, atomically saves the whole staged profile, rolls the runtime mirror back to its exact pre-award state if durable save fails, and restores the exact persisted stat state on load. Production XP mutation remains fail-closed because no authoritative `LevelProgressionPolicy` resource or concrete XP reward amounts/source definitions exist yet: persisted XP semantics, Level-10 overflow behavior, per-transition skill-point quantities, exact automatic-stat field/base/growth values and concrete main/side/boss/encounter/elite/exploration XP amounts/reward abstraction are still unauthored. These UI images remain provisional V01 art until stronger/generated replacements exist.
- The Tenth Warden has a first-pass 96×96 visual inventory for idle, phase two, Twin Cut, Warden Lunge, Arc Volley, Crescent Sweep, Punishing Step, hit and death. It has a dedicated inspector-authored `TenthWardenVisualProfile`, external `AnimationLibrary`, semantic move-binding resources, a `Sprite2D + AnimationPlayer` scene and shared telegraph presenter slot. Authoritative `TenthWardenEncounterState` signals drive move/phase/weak-point presentation through `TenthWardenPresentationStateBinder`, while `TenthWardenCombatRuntime` composes that state with the existing `CombatEncounterRuntime`: the boss registers through the normal enemy lifecycle, consumes one FULL-AI slot, uses normal attack reservations/contact admission/DR-06 damage, enforces the approved Arc Volley/Crescent/Twin Cut semantic constraints, observes the live 50% transition threshold without healing/reset, exposes the Phase-2 weak point only during heavy recovery, and preserves simultaneous-death failure semantics. `TenthWardenSanctum` now supplies a fixed inspector-authored Boss Sanctum shell with four static readable boundaries, separated player/boss spawns, no pits/ambient-damage areas/shrinking walls, and direct preparation of the genuine generic encounter + boss-specific runtime/presentation binding. Renderer evidence: `docs/evidence/renderer/tenth-warden-sanctum-v01.png`. `TenthWardenSanctum` now also owns a one-shot terminal outcome boundary: valid boss defeat while the player remains alive commits `victory`, simultaneous defeat commits `failed_attempt`, and encounter ownership is released exactly once without granting a reward in the Sanctum. `Floor10PrimaryBossObjectiveService` converts only authoritative victory into the Floor 10 primary objective, persists Tenth Warden defeat + Floor 10 clear at declared turn-in, and explicitly does **not** unlock Floor 11 or fabricate a boss-clear reward. Final telegraph geometry, final boss art, authored timing/damage tuning and tower-floor arrival/exit presentation remain open.
- Every generated local PNG is appended to `docs/ASSET_PROVENANCE_MANIFEST.json`; the provenance gate was updated only to remove its obsolete assumption that exactly three art files could ever exist. Metadata validation requirements remain unchanged.
- Gameplay greybox presentation constants that are authoring/tuning values are no longer buried in `_draw()`: `world_canvas_size`, grid cell size, background/grid colors and line width are exported on `GameplayRoot`, and the camera bounds consume the same inspector-authored canvas size. `tests/test_gameplay_presentation_inspector_authoring.gd` locks that boundary while leaving algorithmic camera/combat constants in code.
- Art direction companion docs exist for enemies, Region 3, tower, UI and VFX under `docs/art/`. The remaining exact-source conversion and inspector-first integration rules are specified in `docs/art/ART_INTEGRATION_RULES.md`. The upstream `aldegad/sprite-gen` v2.2.0 guide/tooling is vendored at `D:/nicejourney/tools/sprite-gen` (commit `ed960ac2e8c61b34e34dd47650e4a7a0182cbc0f`) with its dedicated `.venv`; `docs/art/SPRITE_GEN_BUILDING_ASSET_GUIDE.md` and `REGION3_FUNCTIONAL_BUILDING_V02_BATCH.md` specialize that workflow for Region 3 building production. Its local provider probe currently reports Codex CLI/Grok credentials unavailable, so it is being used for workflow/processing/QA guidance rather than as a hidden provider fallback; the Codex orchestrator remains disabled.

Executable gates: `tests/test_player_body_animation_assets.gd`, `tests/test_player_presentation_inspector_authoring.gd`, `tests/test_gameplay_presentation_inspector_authoring.gd`, `tests/test_enemy_presentation_inspector_authoring.gd`, `tests/test_remaining_enemy_presentation_inspector_authoring.gd`, `tests/test_enemy_full_archetype_catalog.gd`, `tests/test_combat_effect_presentation_inspector_authoring.gd`, `tests/test_region3_functional_building_visuals.gd`, `tests/test_region3_authored_town_layout.gd`, `tests/test_region3_route_connectivity.gd`, `tests/test_tower_room_visuals.gd`, `tests/test_tower_room_visual_showcase.gd`, `tests/test_tenth_warden_presentation_inspector_authoring.gd`, `tests/test_tenth_warden_runtime_presentation_binding.gd`, `tests/test_tenth_warden_combat_runtime_integration.gd`, `tests/test_tenth_warden_sanctum_integration.gd`, `tests/test_floor10_primary_boss_objective.gd`, `tests/test_enemy_runtime_presentation_binding.gd`, `tests/test_player_defense_outcome_effect_binding.gd`, `tests/test_player_runtime_body_animation_binding.gd`, `tests/test_ui_icon_catalog_inspector_authoring.gd`, `tests/test_visual_asset_inventory.gd`, `tests/test_asset_provenance_manifest.gd`, `tests/test_m1.gd`, `tests/test_starter_class_combat.gd` and `tests/test_project_resource_paths.gd` — **PASS**. Inspector-authoring gates cover player/gameplay presentation, all twelve enemy archetype presentation scenes, the Tenth Warden presentation shell, all 19 generated V02 VFX/telegraph profiles, Region 3 functional visuals plus its 20-anchor/entrance/route authored-town shell, tower room categories/showcase and all 28 current UI icon assets without runtime frame stepping or hardcoded art paths. Latest repository-wide regression `docs/evidence/reference-validation-latest.json` discovered **159** tests and reported **0 failures**; editor parse, main-scene smoke and renderer smoke PASS. The live player now owns checkpoint-restored HP/stamina/mana/cooldown state and a real health component, authoritative tower encounters bind their player combatant state back to those live resources, death/retry reconstructs the latest durable safe generation, and the generated tower floor now derives room-entry encounter triggers from authored walkable geometry. Floors 1–10 also have deterministic reversible prototype encounter allocations plus explicit playtest enemy runtime tuning; these are mechanically live but remain non-final content/balance. The current renderer timing/memory sample is recorded in that evidence file rather than duplicated here. The expected invalid-profile log remains a negative save-test fixture, not a failure. Region 3 decorative structures use twelve inspector-owned `Region3DecorativeBuildingVisualProfile` resources collected by `Region3DecorativeBuildingVisualCatalog`. Decorative 01–10 are now accepted generated-source-derived V02 at 160×160 from `D:/nicejourney/Nice_Journey_Region3_Decorative_V02_Batch1.zip` (SHA-256 `ec1ff43c98b74e4d87c4b9782b043947d18e8375868140267db3fa9ab512580b`): exact sources and raw ZIP are preserved, edge-touching sources are normalized only with transparent padding, deterministic hashes/provenance are recorded, profiles 01–10 point to V02, renderer evidence is `docs/evidence/renderer/region3-authored-town-layout-v02-decorative-batch1.png`, and superseded V01 01–10 files/provenance were removed only after green focused + broad validation. Decorative 11–12 currently point to V02 preview derivatives, but Batch 2 remains pending exact-source acceptance/provenance; `tools/art/region3_decorative_support_v02_intake.py` supports the approved staged Batch-1/Batch-2 filename contracts.

Open visual acceptance work: representative renderer-active screenshots/video, combat-event hookups for non-basic player actions, genuine live enemy combatant instantiation/binding, **Region 3 decorative/support Batch 2 exact-source acceptance/provenance** (decorative 11–12 + props/ground-road/ruins/outskirts/risk-zone), quest-specific escort actor/goal/failure-policy binding over the already-green navigation/rejoin/follow infrastructure and final environment dressing, stronger/generated tower/Tenth-Warden/UI replacements, Region quest/risk map-state ownership plus broader graphical/UI map consumption, final polish/variation, Tenth Warden production authoring/defender-facts ownership plus final telegraph/art, audio content and DR-08 final performance certification. Batch 1 decorative 01–10 is accepted/integrated; PNG inventory or presentation-resource existence alone is still not treated as final-art approval.

## Current implementation boundary

DR-01 through DR-08 are no longer approval blockers. Production class kits, shared damage/defense, skill content, retry/death restoration, quest packaging, replay economy, the Floor 10 boss and final performance certification may now be implemented when their prerequisite systems are ready.

New work must implement the exact resolved §58 contracts rather than inventing additional mechanics, while tooling, validation, observability and generic boundaries may continue in parallel. Waves 24A/24B and 25–29 are implemented and prime-validated; Wave 24B still needs an independent read-only QA pass when the non-Codex worker path is available. Wave 30 player presentation remains complete; all 17 player-body categories have since been replaced by visually classified exact-source-derived V03 sheets, while starter equipment remains exact-source V02 at the asset level; all twelve reusable enemy archetypes now use generated-source-derived V02 gameplay art through Sprite2D/AnimationPlayer/inspector-profile presentation shells, and the full 12-archetype §22.7 threat-card catalog is now mechanically defined/validated (including bounded Summoner reinforcement constraints, visible/reachable Flying Harrier rules and Controller/Disruptor Slow-field counterplay); the generated V02 player-VFX/telegraph set has inspector-first presentation architecture; Region 3/tower visual categories, the Tenth Warden presentation shell and the current UI icon inventory are inspector/resource-owned. **All further visual integration must follow the Sprite2D + AnimationPlayer / inspector-first resource pattern where sprite presentation is appropriate rather than runtime frame loading or hardcoded presentation constants.** The remaining art/content sequence is Region 3 decorative/support environment production, stronger/generated tower replacements, Tenth Warden final generated art/telegraphs on the already-authoritative boss runtime, broader UI final art/consumption, renderer evidence, polish, then DR-08 final certification.

## Current reusable enemy runtime continuation

- `EnemyPrototypeMovementDriver` executes authored approach/withdraw/reposition intent through `NavigationAgent2D` and now projects requested destinations onto the synchronized authored navigation map before movement. This prevents wall/boundary probes from becoming off-navigation movement targets while preserving connector/route navigation where legitimately authored.
- Arrival tolerance is enforced before stepping, preventing close-range navigation jitter. Lost-sight/reposition behavior remains snapshot-owned: movement never starts following a target node after the admitted observation changes.
- `tests/test_enemy_prototype_movement_driver.gd` now exercises flat-map arrival/boundary behavior and a live generated tower combat room, including wall projection, off-route withdraw clamping, synchronized-map membership and no unseen-target pursuit.
- Signature phase execution now exposes a read-only ACTIVE delivery context only when the exact reservation token and shared attack-pressure commitment still match the same encounter/actor/target/action-instance. `AttackReservationLedger` and `AttackPressureLedger` provide exact ownership queries, `CombatEncounterRuntime` composes them, `EnemyArchetypeRuntime` admits the ACTIVE delivery context, and `EnemySignatureActionPhaseDriver` opens the window exactly once at ACTIVE entry. `TowerEncounterSessionHost` forwards the verified context without adding an unauthored attack payload.
- `EnemyActiveAttackDeliveryExecutor` is the generic authoritative bridge from that ACTIVE window into DR-06 direct-hit resolution. It requires a caller-supplied attack payload and caller-resolved contact/defense facts, revalidates the exact live delivery context immediately before contact, preserves `CombatContactLedger` duplicate protection, and routes a successful parry's authored attacker-interrupt request through the same signature phase driver. It does not choose damage numbers, damage domain, contact/projectile type, hit geometry, or hit timing.
- `tests/test_enemy_active_attack_delivery_executor.gd` covers windup rejection, invalid-payload non-consumption, duplicate-contact protection, accepted ACTIVE delivery, parry interruption/ownership release, fatal-target invalidation and phase-owner reconciliation. Tower session-host coverage proves one executor is bound to each live reusable-enemy runtime/phase-driver pair.
- Floors 5 and 10 also have live regression coverage proving exactly three elite identities retain the existing reversible HP/Physical-defense/Arcane-defense/poise modifiers while every active resident—elite or normal—uses ordinary counted FULL-AI admission and no elite identity fabricates attack pressure.
- `EnemyPrototypeDecisionTuning` now includes reversible signature-repeat suppression (`signature_repeat_limit = 2`, `signature_suppression_evaluations = 1`) to satisfy the locked internal action-budget anti-spam requirement without inventing new defensive actions. `EnemyPrototypeDecisionDriver` counts only successful signature commitments, applies suppression only once the actor is idle and cooldown-ready, and then reopens normal signature utility after the configured suppressed evaluation is consumed. Range/objective tactics retain priority and remain available during suppression.
- Signature commitment also retains the exact admitted `observed_target_position` as a read-only committed-observation snapshot. `EnemyArchetypeRuntime` carries that snapshot into ACTIVE delivery context and clears it with action ownership; later perception updates cannot rewrite an already committed action's observation. This provides fair input for future authored retarget/geometry rules without claiming that every archetype uses a universal hard aim lock.
- Tenth Warden direct-contact delivery now rejects while its existing recovery state is active, and a successful DR-06 parry of a parryable boss contact enters that same recovery state so subsequent recovery contacts are impossible. No boss timing/damage values were added.
- Focused enemy movement/reservation/pressure/signature/delivery/decision/session/elite/boss tests, editor parse and the repository-wide gate remain green.
- Exact reusable-enemy attack damage, damage domain/delivery tags, hit geometry and telegraph-to-hit timing remain unimplemented where the master/resources do not author those values. Presentation telegraph resources are not treated as combat-authority data.

## Validation commands

Using the locally verified engine:

```text
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --editor --quit
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_m0.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_m1.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_m2_foundation.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_combat_contact_ledger.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_direct_hit_resolver.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_poise_resolver.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_prototype_status_resolver.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_action_input_integration.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_audio_foundation.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_audio_lifecycle.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_audio_settings.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_danger.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_input_bindings.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_input_ownership.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_operation_guard.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_save_request_coordinator.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_save_compatibility.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_profile_creation.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_main_profile_flow.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_claim_ledger.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_pause_coordinator.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_accessibility_settings.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_ai_observation_admission.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_ai_observation_contract.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_full_ai_simulation_ledger.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_enemy_root_lifecycle.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_attack_reservation_ledger.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_attack_participation_admission.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_region3_town_structure_manifest.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_world_region_catalog.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_map_layer_identity.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_prototype_tower_floor_catalog.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_equipment_slot_identity.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_interaction_commit_admission.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_protected_item_operation_policy.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_normal_inventory_capacity_policy.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_normal_stack_quantity.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_development_observability.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_asset_provenance_manifest.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --script res://tests/test_project_resource_paths.gd
C:\Users\LENOVO\Desktop\GodotC#\Godot_v4.6.2-stable_mono_win64_console.exe --headless --path . --quit-after 2
```

Previous repository-wide validation after the persisted-binding conflict hardening: editor parse, all 15 then-discovered `test_*.gd` gates (including neutral action-intent/modal integration), and configured main-scene launch — **PASS**. Evidence: `docs/evidence/reference-validation-wave6.json` (renderer smoke intentionally skipped for that functional regression pass).

Current §32.3 save-request coordinator validation: editor parse plus `tests/test_save_request_coordinator.gd`, `tests/test_m0.gd` and `tests/test_operation_guard.gd` — **PASS**.

Current §33.1/§35 UI/text scaling validation: editor parse plus `tests/test_accessibility_settings.gd`, `tests/test_main_profile_flow.gd` and `tests/test_pause_coordinator.gd` — **PASS**. The accessibility gate covers persistence, UI-only scaling, world-camera zoom independence, 640×360 maximum-scale layout/focus reachability and non-finite scale recovery.

Current fair-AI observation/admission/memory validation: editor parse plus `tests/test_ai_observation_admission.gd`, `tests/test_ai_observation_contract.gd` and `tests/test_development_observability.gd` — **PASS**. It covers canonical sight/sound/shared admission, original shared observation age/confidence/lifetime preservation, expired-at-admission and malformed-fact rejection without mutation, bounded latest-by-observation-time memory, unseen-movement isolation, reaction expiry, detached debug enumeration, and absence of raw-input/live-target/hidden/tactical/combat-resolution fields.

Current §23/§23.1 FULL-AI simulation admission validation: editor parse plus `tests/test_full_ai_simulation_ledger.gd` and adjacent `tests/test_enemy_root_lifecycle.gd` — **PASS**. The simulation gate covers the locked global hard cap of 12 across overlapping encounter IDs, duplicate/invalid actor rejection, explicit idempotent-safe release, released-capacity reuse, deterministic detached debug enumeration and separation from lifecycle/tactics/pressure/scheduling/reset policy.

Current §23.1 Active Combat state validation: editor parse plus `tests/test_active_combat_registry.gd`, `tests/test_operation_guard.gd`, `tests/test_full_ai_simulation_ledger.gd` and `tests/test_enemy_root_lifecycle.gd` — **PASS**. The registry covers all four locked active-combat causes, stable source IDs, duplicate-safe source/reason ownership, explicit independent release, deterministic detached debug reasons, and separation from operation blockers, distance/LOS clearing, grace timing, music/tactics/damage/content/save triggering and DR-01/02 policy.

Current §22.7 attack-reservation validation: editor parse plus `tests/test_attack_reservation_ledger.gd`, `tests/test_enemy_root_lifecycle.gd`, `tests/test_full_ai_simulation_ledger.gd` and `tests/test_active_combat_registry.gd` — **PASS**, including independent worker QA. Coverage includes stable actor/target/encounter/action-instance identity, atomic duplicate-safe reservation acquisition, the exact six release causes, death/target-invalidation/encounter-exit bulk scoping, deterministic detached debug data and absence of pressure/tactics/damage/cooldown/DR-01/02 policy.

Current §22.7/§23.1 attack-participation composition validation: editor parse plus `tests/test_attack_participation_admission.gd`, `tests/test_enemy_root_lifecycle.gd`, `tests/test_full_ai_simulation_ledger.gd`, `tests/test_attack_reservation_ledger.gd` and `tests/test_active_combat_registry.gd` — **PASS**, including independent read-only QA. Coverage proves Active/reservation eligibility, exact same-actor/same-encounter FULL-AI ownership, delegated reservation identity rules, and source-state non-mutation on rejection without adding runtime encounter ownership or pressure/tactic/damage/reset policy.

Current §24.4 / §61 QA-REGION validation: the original pure structure-manifest gate remains independently QA-passed, and `tests/test_region3_authored_town_layout.gd` adds executable coverage for the master §24.12 authored greybox hypothesis. It verifies all 20 stable anchors, exact reserved-lot centers, 8-functional/12-decorative coverage, the 160×160 / town / plaza reservations, all eight functional entrance/approach tiles against door-facing lot boundaries, one closed three-tile constrained inner loop, four outward approaches at the four-tile target, and three-tile service connectors. `tests/test_region3_collision_traversal.gd` now extends that placement/topology evidence with real physics coverage: one authored collider per structure, full inclusive min/max tile occupancy, open working entrances/approaches, clear route centerlines, reachable main approaches/service approaches, fixed world-edge collision, and real-player-footprint sampling using the live 12×10 `player.tscn` collider along every authored route and functional entrance/approach. `tests/test_region3_route_navigation_graph.gd` additionally proves the scene-owned route navigator compiles one connected path graph, reaches the Central Tower from every functional/main approach, rejects disconnected geometry, does not silently snap off-network starts, and returns explicit bounded rejoin plans without mutating actor position or persistent graph state. Actual moving escort/NPC follow behavior, authored escort actor/goal/failure-policy content, quest/service behavior and final ground composition remain separate work.

Current §§24.1–24.7 / §61 QA-MAPS world-region catalog validation: editor parse plus `tests/test_world_region_catalog.gd` and `tests/test_project_resource_paths.gd` — **PASS**, including independent read-only QA. Coverage proves exactly 12 integer region IDs 1–12 once each, Region 3 as the sole `prototype_starting_region`, the other 11 as `future_locked`, optional non-authoritative names, deterministic malformed-data rejection and caller-input non-mutation without adding travel, discovery, layout or other-region gameplay policy.

Current §24.6 / §61 QA-MAPS map-layer identity validation: editor parse plus `tests/test_map_layer_identity.gd` and `tests/test_project_resource_paths.gd` — **PASS**, including independent read-only QA. Coverage proves exactly the three separate `world_map` / `region_map` / `tower_floor_map` identities with deterministic malformed/missing/unknown/duplicate rejection and non-mutating validation. Runtime navigation, discovery, markers, travel and floor-content behavior remain unclaimed.

Current §4.1 / §4.5 prototype tower-floor catalog validation: editor parse plus `tests/test_prototype_tower_floor_catalog.gd` and `tests/test_project_resource_paths.gd` — **PASS**, including independent read-only QA. Coverage proves exactly the integer Floor 1–10 identity set once each, order-independent, with optional non-authoritative names, deterministic malformed/duplicate/missing/out-of-range rejection and caller-input non-mutation. Unlock, generation, quest, boss, replay/reward and map-runtime behavior remain unclaimed.

Current §20.1 / §20.6 equipment-slot identity validation: editor parse plus `tests/test_equipment_slot_identity.gd` and `tests/test_project_resource_paths.gd` — **PASS**, including independent read-only QA. Coverage proves the five locked slot identities exactly once in any order, including two distinct accessory slots, with deterministic malformed/missing/unknown/duplicate rejection and caller-input non-mutation. Equip compatibility/transactions, items, stats, starter-kit resources and combat semantics remain unclaimed.

Current §25.1 interaction commit admission validation: editor parse plus `tests/test_interaction_commit_admission.gd` and `tests/test_project_resource_paths.gd` — **PASS**. Coverage proves all seven caller-resolved commit preconditions are revalidated, admitted logical commit IDs are duplicate-safe, failed admission does not consume the ID, independent IDs remain isolated, malformed context rejects deterministically and caller context is not mutated. Target arbitration, spatial calculations, interaction effects and runtime ownership remain unclaimed.

Current §26.1 / §26.2 protected-item operation validation: editor parse plus `tests/test_protected_item_operation_policy.gd` and `tests/test_project_resource_paths.gd` — **PASS**, including independent read-only QA. Coverage proves protected quest/key items reject drop/destroy/sell, ordinary quest removal remains permitted for non-Sigil protected items, the Tower Sigil rejects every removal path including quest removal, malformed inputs reject deterministically, and caller state is not mutated. This is not full inventory-operation admission; ownership, confirmation, capacity, stacking, transactions and runtime effects remain unclaimed.

Current §26 / §26.2 normal-inventory capacity validation: editor parse plus `tests/test_normal_inventory_capacity_policy.gd` and `tests/test_project_resource_paths.gd` — **PASS**, including independent QA. Coverage proves the 16-slot normal capacity boundary at 0/15/16 occupancy, protected quest/key bypass, caller-resolved no-new-slot behavior at full capacity, malformed/out-of-range fact rejection, deterministic output and non-mutation. Stack compatibility/quantity resolution and actual inventory mutation remain caller/runtime-owner responsibilities.

Current §26 / §26.2 normal-stack quantity validation: editor parse plus `tests/test_normal_stack_quantity.gd`, the adjacent equipment/protection/capacity gates and `tests/test_project_resource_paths.gd` — **PASS**, including independent read-only QA. Coverage proves strict integer realized-stack quantities from 1 through 99 inclusive, rejection of zero/negative/100+/non-integer values, deterministic repeated validation and caller-value non-mutation. Stackability, compatibility, transactions and inventory ownership remain outside this boundary.

Current §61.1 development observability validation: editor parse plus `tests/test_development_observability.gd`, `tests/test_action_input_integration.gd`, `tests/test_combat_contact_ledger.gd`, `tests/test_operation_guard.gd`, `tests/test_save_request_coordinator.gd` and `tests/test_ai_observation_contract.gd` — **PASS**. The composition gate covers cross-system action/contact correlation, guard source identity, save slot/request identity, detached read snapshots, bounded deterministic AI enumeration with explicit age/confidence/expiry checks, forbidden-field absence and source-state non-mutation.

Current save compatibility/recovery observability validation: `tests/test_save_compatibility.gd`, `tests/test_m0.gd` and `tests/test_save_request_coordinator.gd` — **PASS**. Unsupported schema is distinguished from malformed JSON, a prior-valid generation is used only as fallback, detailed status reports the real backup state even when current is valid, invalid slots return a stable failure without a fabricated profile, and slots remain isolated. These tests predate and do not yet exercise the now-approved DR-01 replay / DR-02 death-retention behavior.

Current §37.1 native-audio/settings validation: editor parse plus `tests/test_audio_foundation.gd`, `tests/test_audio_lifecycle.gd`, `tests/test_audio_settings.gd`, `tests/test_accessibility_settings.gd`, `tests/test_input_bindings.gd`, `tests/test_input_ownership.gd`, `tests/test_pause_coordinator.gd` and `tests/test_main_profile_flow.gd` — **PASS**. Coverage includes authored lifecycle accounting, direct non-finite native-volume rejection, all five persisted bus volume/mute settings, immediate application, independent persistence/reload, malformed recovery, atomic invalid-binding rejection, audio-only reset isolation, campaign-slot separation, and keyboard reachability in both pre-game and pause at 640×360 with maximum current UI/text scaling. No mono simulation, final assets/catalog, priority stealing, adaptive music or sound-off comprehension claim is added.

Current §43 localization-readiness validation: editor parse plus `tests/test_main_profile_flow.gd`, `tests/test_accessibility_settings.gd` and `tests/test_input_bindings.gd` — **PASS**. Dynamic player-facing strings use English source messages through `tr()` while stable class/action IDs remain unchanged. English-only prototype scope remains explicit; no translated catalog or additional locale support is claimed.

Provisional renderer-active visual evidence is documented in `docs/VISUAL_EVIDENCE_PROVISIONAL.md` with captures under `docs/evidence/visual/`. The minimum supported **1280×720 output**, separate **640×360 internal-canvas** diagnostic, current max-scale settings surface, and one real **1920×800 physical-client ultrawide final-window composition** case now have direct evidence. That ultrawide case preserves a 1280×720 rendered surface with black bars/no wider greybox world frame and passed independent visual/tooling review. Additional aspect-ratio coverage, undiscovered-room/objective leak validation, representative raster/pixel-art, broader max-scale accessibility and final visual acceptance remain open.

Latest repository-wide functional regression against the **resolved §58 master**: `docs/evidence/reference-validation-latest.json` — **PASS** on Godot 4.6.2 Mono `71f334935`, `gl_compatibility`: editor parse, all **191** dynamically discovered `tests/test_*.gd` gates, configured main-scene smoke, and renderer smoke reported **0 failures**. Reference smoke: p50 **16.657 ms**, p95 **17.02 ms**, p99 **17.222 ms**, max **17.748 ms**, peak static **58,749,718 bytes**. The recurring `Save rejected: invalid profile_id` line is the intentional negative fixture and occurred with zero aggregate failures. The gate includes current player V03 presentation and runtime animation reachability coverage, all twelve generated-V02 enemy inspector scenes including the accepted remaining-eight batch, corrected Duelist and remaining-eight derivation/provenance data, gameplay presentation exports, the generated V02 Region 3 functional-building batch with exact-source/normalized-source/derivation/provenance records and updated service-door/route anchors, tower inspector catalogs/showcases, the 20-anchor + connected-route authored Region 3 shell, and Tenth Warden authoritative runtime-state signals driving its inspector-owned `Sprite2D + AnimationPlayer` presentation through a dedicated binder, plus the boss-specific combat composition layer using the existing encounter/FULL-AI/reservation/contact owner without hardcoded art paths or guessed telegraph geometry, plus the 28-entry UI icon catalog and comparison showcase. All twelve generated-V02 enemy visual scenes also own a shared runtime-presentation binder for authoritative block/hit/death results, and the player scene owns inspector-authored defense-outcome VFX bindings so live Block/Parry/Guard Break results select the generated block spark/parry spark/guard-break cue without hardcoded art paths or manual frame stepping. Real renderer captures include `docs/evidence/renderer/main-profile-ui-1280x720.png`, `docs/evidence/renderer/gameplay-v02-player-1280x720.png`, `docs/evidence/renderer/generated-v02-enemy-showcase-fixed-duelist.png`, `docs/evidence/renderer/remaining-eight-enemy-showcase-v02.png`, `docs/evidence/renderer/region3-authored-town-layout-v02-functional-buildings.png`, `docs/evidence/renderer/tower-room-visual-showcase-v01.png`, and `docs/evidence/renderer/tenth-warden-sanctum-v01.png`. Region 3 functional landmarks are generated V02; the Region 3 decorative/support environment, tower, Tenth Warden and UI categories remain explicitly non-final where still V01. **Final visual acceptance and DR-08 minimum-hardware certification are still not claimed.**

## Tooling note

Wave 24B was implemented directly through Sol/Auvrynt without using the Codex orchestrator, per current developer instruction. A non-Codex Chat On Steroids worker was attempted only for the independent read-only QA gate, but its tunnel client was disconnected, so that independent gate remains pending. This is a tooling limitation, not a game-code dependency; the direct implementation, focused tests, editor parse and 41-test repository-wide prime validation are green.

A Serena semantic-audit session was also attempted, but its GDScript language server could not connect to `127.0.0.1:6008`. No workaround was attempted. Godot 4.6.2 editor parsing and executable headless tests remain the validation evidence for this pass.
